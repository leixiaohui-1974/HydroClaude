# 非恒定流根本问题分析

**日期**: 2025-10-26  
**发现**: 稳态初始值与非恒定流Preissmann步不兼容

---

## 🔬 关键发现

### 实验结果

```
测试：use_pump_mask=False + conserve_local_flow=True (pass)
结果：数值发散（NaN）

测试：use_pump_mask=False + 完全不调用内部BC  
结果：仍然发散（NaN）
```

### 结论

**问题不在泵站处理方法，而在稳态初始值！**

---

## 🎯 根本原因

### 稳态初始值特征

泵站附近水深分布：
```
idx24: h=2.21 m, Q=10.0 m³/s  (上游)
idx25: h=3.71 m, Q=10.0 m³/s  (泵站中心)
idx26: h=5.21 m, Q=10.0 m³/s  (下游，跳跃后)
idx27: h=4.96 m, Q=10.0 m³/s  (过渡区)
```

### 问题

**水深跃变** (2.21m → 5.21m) + **恒定流量** (10 m³/s)  
→ **流速剧变**: u₂₄ = 10/(10×2.21) = 0.45 m/s → u₂₆ = 10/(10×5.21) = 0.19 m/s

这种配置在稳态中通过**掩码+全局流量强制**维持，但在非恒定流中：
- 如果使用掩码：泵站节点不更新 → 流量累积
- 如果不使用掩码：水深梯度太大 → 压力波 → 数值发散

---

## 💡 根本解决方案

### 方案1: 使用兼容的稳态初始值（推荐）

**核心思想**：为非恒定流准备一个"平滑"的初始条件

```python
# 稳态初始化后，平滑泵站跳跃
if running_transient_later:
    # 将5m跃变分散到更大范围
    idx_pump = 找到泵站
    平滑范围 = idx_pump ± 10节点
    
    # 线性过渡
    h_smooth = np.linspace(h_upstream, h_downstream, 21)
    solver.h[idx_pump-10:idx_pump+11] = h_smooth
```

### 方案2: 渐进施加泵站跳跃

**核心思想**：在非恒定流前几百步中逐步建立泵站跳跃

```python
# 非恒定流开始时，泵站跳跃系数从0渐进到1
for i in range(nt):
    alpha = min(1.0, i / 100.0)  # 前100步渐进
    
    # 施加部分跳跃
    h_downstream = h_upstream + alpha * rated_head
```

### 方案3: 使用源项法（需重构稳态求解器）

不使用跳跃条件，改用源项法。

---

## 📋 推荐实施

### 立即行动：方案1（平滑初始值）

1. 恢复use_pump_mask=True（避免发散）
2. 在非恒定流开始前平滑泵站跳跃  
3. 接受初期流量累积（会逐步消散）

### 预期效果

- 数值稳定：✓ 不发散
- 流量守恒：初期差，逐步改善到5%以内
- 泵站扬程：在稳态中正确（100%），非恒定流中逐步建立

---

*分析时间: 2025-10-26*
*下一步: 实施方案1*
