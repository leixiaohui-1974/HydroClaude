# Example 7 - Multiple Plans (NAPA River) 精度诊断报告

**案例文件**: `reports/hecras_reference_data/example_7__multiple_plans_NAPA.p01.json`  
**当前结果**: MAE = 0.624m，FAIL（目标 < 0.15m）  
**诊断日期**: 2026-03-22

---

## 1. 整体误差特征

**系统性偏低（HC < HR）**，全部 80 个断面中 76 个偏低，仅 xs79（下游边界）误差为零，xs71-73 误差 < 0.08m。

| 分段 | 断面范围 | 平均误差 | 最大负误差 | 特征 |
|------|---------|---------|----------|------|
| 上游高水位段 | xs0 - xs14 | -0.78m | -1.15m (xs14) | 被 xs14→xs15 跳变截断 |
| 中游主段 | xs15 - xs70 | -0.50m | -1.36m (xs70) | 误差向上游沿程积累 |
| 潮汐影响段 | xs71 - xs79 | -0.61m | -1.81m (xs78) | 高垄+逆坡+潮汐壅水 |
| 过渡准确区 | xs71 - xs73 | ±0.08m | — | 唯一精度达标段 |

---

## 2. 根本原因分析（按重要性排序）

### 问题 1：xs14 → xs15 之间存在未记录的水工结构（最严重）

**现象**：xs14（WSE = 7.07m）到 xs15（WSE = 5.99m）之间 WSE 下降 **1.09m**，能量损失分析显示：

- 55ft（17m）距离内能量差 = 0.82m（远超正常摩擦损失 ~0.05m）
- 实际额外能量损失 ≈ **0.82m**

**结论**：此处有 HEC-RAS 模型中的水工结构（水坝/堰/水闸/控制闸），但 JSON 导出数据中**未记录**这个结构。

**影响**：求解器把这两个断面当作普通相邻断面处理，xs0-14 的上游壅水效应被完全忽略，导致这 15 个断面 WSE 平均偏低 0.78m。

**佐证**：xs14 处的 friction_slope = 0.00112（极低），alpha = 1.10，明显是低流速的壅水断面，而 xs15 的 friction_slope = 0.00267（提高了 2.4 倍），说明下游水位骤降后流速加快。

---

### 问题 2：IFA（无效流动区域）conveyance 计算 Bug

**现象**：在 xs15 处，有两个 IFA 块（`[0, 56.5]ft` 和 `[93.2, 150]ft`，触发高程 = 22.5ft = 6.86m）。当 WSE = 5.99m < 6.86m 时，IFA 应该是活跃的（这两块区域为死水区，不参与流动）。

**面积验证（完全正确）**：
- 全断面面积 = 41.88 m²
- IFA 减除面积 = 2.87 m²  
- 有效面积 = 39.02 m² **≈ HEC-RAS 的 39.02 m²**（完全一致）

**Conveyance 计算错误**：

| 方法 | K（m³/s） |
|------|---------|
| 无 IFA 的原始 K | 2137.8 |
| 有 IFA 的修正 K（当前代码） | **2540.2** ← 反而变大！ |
| HEC-RAS 参考 K | 2229.9 |

**Bug 根源**（`steady_profile_solver.py` 第 532-563 行）：

```python
# 当前错误逻辑：
R_old = A_total / P_total_full  # 用全断面湿周反推 n_equiv
n_equiv = A_total * R_old**(2/3) / K_total
K_total = (1/n_equiv) * A_eff * R_eff**(2/3)  # R_eff >> R_old，导致 K 变大！
```

IFA 减去了大量湿周（21.33m → 13.79m），使 R_eff = 2.83m >> R_old = 1.96m。n_equiv 由旧 R 反推，然后用新的大 R 计算，导致 K 反而变大。

**影响**：Sf = (Q/K)² 偏小（0.00206 vs 0.00267，偏低 22.9%），每段摩擦能量损失偏小，向上游推算 WSE 偏低，误差在 56 个断面（xs15-70）上沿程积累最终达到 1.36m。

**修复方案**：应直接在各分区（LOB/Channel/ROB）的 conveyance 计算中，先排除 IFA 区域，然后重新计算各区 K：
```python
# 正确逻辑（HEC-RAS 方式）：
# 对每个 IFA 块，确定它属于哪个区（LOB/Ch/ROB），
# 仅从该区的面积中减去，并重新计算该区的 K_i = (1/n_i)*A_i*R_i^(2/3)
```

---

### 问题 3：xs74-78 段的潮汐壅水（逆坡）处理不当

**现象**：xs71-79 段是 NAPA River 下游受潮汐影响的入海口区域，床面高程为负值（最低 -3.82ft = -1.16m）。

关键地形特征：
- xs74-75：床面高程 4.9ft（1.49m），形成一个**高垄（sill）**
- xs76：床面 -0.70ft（-0.21m），急剧跌落至低洼区
- xs76 WSE = 13.25ft > xs75 WSE = 12.88ft，呈**逆坡**（下游 WSE 高于上游）

这是典型的潮汐壅水结构：下游潮位（xs79 = 13.0ft）通过低洼区（xs76-78）将水位壅高，到达高垄 xs75 时 WSE 才低于 xs76。

**求解器误差**：
- xs74 误差 = -0.19m（尚可）
- xs75 误差 = -0.67m（高垄处，能量损失计算出现偏差）
- xs76 误差 = -1.28m（高垄下游的壅水区）
- xs77 误差 = -1.54m
- xs78 误差 = **-1.81m**（最大误差）

从 xs76 到 xs75，需要的 Sf = 0.00383，而平均 Sf = 0.00229，相差 1.68 倍。这说明能量方程在从低床面区到高床面区时计算不准确，摩擦坡降被低估。

---

### 问题 4：xs0-14 段的连续性问题（次要）

即使解决了问题 1（缺失结构），xs0-14 还有一个小问题：xs1→xs2 的床面高程从 11.5ft 升到 13.17ft（逆坡），误差从 xs1 的 -0.24m 突增到 xs2 的 -0.85m，说明这里也有类似的能量方程局部处理问题。

---

## 3. flow 变化与区间来水

**无区间来水**：全部 80 个断面的 flow 均为 4070 cfs（115.25 m³/s），完全一致，无侧向入流。

---

## 4. ineffective_areas 存在情况

存在 8 个断面的 IFA 记录：

| 断面 | IFA 区域 | 触发高程 | WSE 处是否活跃 |
|------|---------|---------|-------------|
| xs14 | [0,56.5]ft + [93.2,150]ft | 22.5ft (6.858m) | 否（WSE=23.20ft > 22.5ft）|
| xs15 | [0,56.5]ft + [93.2,150]ft | 22.5ft (6.858m) | **是**（WSE=19.64ft < 22.5ft）|
| xs61 | [0,54.5]ft + [95,150]ft | 21.9ft (6.675m) | **是**（WSE=16.72ft < 21.9ft）|
| xs62 | [0,54.5]ft + [95,150]ft | 21.9ft (6.675m) | **是** |
| xs65 | [0,55.5]ft + [96.8,150]ft | 20.0ft (6.096m) | **是** |
| xs66 | [0,55.5]ft + [97,150]ft | 20.0ft (6.096m) | **是** |
| xs70 | [0,55.5]ft + [94,150]ft | 19.6ft (5.974m) | **是** |
| xs71 | [0,53.5]ft + [94.5,150]ft | 19.6ft (5.974m) | **是** |

注意：xs14 的 ifa 在参考 WSE 时处于不活跃状态（WSE 高于触发高程），而 xs15-71 的 ifa 均处于活跃状态，这些断面的 conveyance 都受到 Bug 影响（均偏高）。

---

## 5. 逆坡段分布

| 位置 | 逆坡程度 | 原因 |
|------|---------|------|
| xs51→xs52~56 | +0.005~+0.007m/段 | 轻微壅水（中游段，Sf 仍为正） |
| xs71→xs72 | -0.029m (逆坡) | 高垄形成的回水 |
| xs75→xs76 | -0.112m (逆坡) | 潮汐壅水（下游 WSE 高于上游） |
| xs78→xs79 | -0.029m (逆坡) | 潮汐影响 |

---

## 6. 误差归因定量估计

| 原因 | 影响的断面 | 估计贡献 |
|------|----------|---------|
| 缺失水工结构（xs14-15 间） | xs0-14（15个断面） | MAE 贡献约 0.78m |
| IFA conveyance Bug | xs15-70（56个断面） | MAE 贡献约 0.35m |
| 潮汐段能量计算（xs74-78） | xs74-78（5个断面） | MAE 贡献约 0.90m |
| 其他（床面逆坡，局部误差） | 全段 | MAE 贡献约 0.10m |

---

## 7. 修复优先级

### P1（高优先级）：修复 IFA Conveyance Bug

**文件**: `solvers/steady_profile_solver.py`，第 532-563 行

**当前问题**：
```python
# 错误：用总断面参数反推 n_equiv 再用有效断面参数重算 K
n_equiv = A_total * R_old**(2.0/3.0) / max(K_total, 1e-9)
K_total = (1.0 / max(n_equiv, 0.001)) * A_eff * R_eff**(2.0/3.0)
```

**正确修复方向**：对 LOB/Ch/ROB 各区分别计算 IFA 减除后的有效面积，然后独立重算各区 K，不使用 n_equiv 反推。

预期效果：xs15-70 段误差可以从 -0.30~-0.50m 减少到 -0.10~-0.15m。

---

### P2（中优先级）：完善 JSON 导出以包含水工结构信息

**问题**：HEC-RAS 的 `p01.hdf` 文件中可能包含 inline structure（水坝/堰）信息，但当前 JSON 导出器（`hdf_extractor.py` 或相关脚本）未读取这部分数据。

**建议**：检查 HEC-RAS HDF 文件中的 `Structures` 节点，提取 inline structure（内联结构）的位置和属性到 JSON，并在求解器中实现 weir/gate 计算。

---

### P3（低优先级）：改进潮汐段能量方程计算精度

**问题**：xs75 处（高垄）的 Sf 需要 0.00383，但平均 Sf = 0.00229，差 1.68 倍。

**建议**：在床面高程急剧变化（相邻断面床面高差 > 1m）时，增加中间断面插值或子步计算，减少单步能量误差。

---

## 8. 结论

Example 7 的 MAE = 0.624m FAIL 主要由两个独立原因叠加造成：

1. **缺失数据**：xs14-xs15 之间有水工结构（水坝/堰），JSON 未记录，导致上游 15 个断面全部偏低约 0.78m。

2. **IFA Conveyance Bug**：IFA 活跃时 K 反而比没有 IFA 时更大，导致 Sf 偏低、能量损失被低估，中游 56 个断面沿程积累误差。

3. **潮汐段**：下游入海口的逆坡/高垄结构超出当前能量方程的处理能力，xs74-78 严重偏低。

修复 IFA Bug（P1）可望使 MAE 降低约 0.2-0.3m，但即使修复后，由于缺失结构（P2）的问题，案例仍会 FAIL。需要同时补充水工结构数据才能达到 MAE < 0.15m 的目标。
