# Phase 2 进展总结：求解器集成

**日期**: 2025-10-23
**状态**: 进行中（遇到技术难点）

## 目标

将Phase 1完成的静水重构法（machine precision well-balanced）集成到实用的渠道求解器中，支持：
- Preissmann隐式时间推进
- 稳态求解
- 闸门等内部边界条件

## 已完成工作

### 1. 核心求解器类创建 ✓

文件: `solvers/hydrostatic_canal_solver.py` (~610行)

**实现功能**:
- ✓ 静水重构通量计算（复用Phase 1的v3算法）
- ✓ HLL Riemann求解器
- ✓ Ghost cells边界条件
- ✓ Audusse源项（良平衡）
- ✓ Manning摩擦项
- ✓ Preissmann隐式时间步（θ=0.6, ω=0.95）
- ✓ 稳态求解器框架

**关键方法**:
```python
class HydrostaticCanalSolver:
    def __init__(...)                      # 初始化
    def reconstruct_interface(...)         # 静水重构
    def hll_flux(...)                      # HLL通量
    def setup_ghost_cells(...)             # 虚拟单元
    def compute_fluxes_and_sources(...)    # 通量和源项
    def step_preissmann(...)               # Preissmann时间步
    def solve_steady_state(...)            # 稳态求解
    def _apply_internal_bc(...)            # 内部边界条件
```

### 2. 基础测试 ✓

文件: `test_hydrostatic_solver.py` (~150行)

**测试场景**: 简单渠道稳态流（无闸门）
- 渠道长度: 1000m
- 网格: 101点
- 目标流量: 10 m³/s

**测试结果**:
- ✗ 流量守恒: 24.23% 误差（目标<5%）
- ✓ 水深精度: 7.21% 误差（目标<10%）
- ✓ 收敛性: 286次迭代收敛

### 3. 闸门边界条件尝试 ⚠️

文件: `test_single_canal_with_gate.py` (~200行)

**实现了3种策略**:

#### 策略1：直接替换界面通量（失败）
```python
# 在compute_fluxes_and_sources中直接替换闸门界面的HLL通量
F_mass[interface_idx] = Q_gate / B
```
**结果**: 流量尖峰，守恒性严重破坏（189%误差）

#### 策略2：强制全渠道流量 + 流量调整（失败）
```python
# 强制所有节点流量
hu_new = (1 - relax) * hu_new + relax * (Q_target / B)
# 然后调整闸门节点流量
```
**结果**: 与闸门约束冲突，闸门流量不匹配（51%误差）

#### 策略3：通过水深调整满足闸门约束（失败）
```python
# 使用牛顿法调整上游水深
dh_up = (Q_target - Q_gate) / (dQ/dh_up)
h[idx-1] += relax * dh_up
```
**结果**: 更差，流量误差102%

## 核心问题分析

### 问题1：稳态流量守恒不佳

**现象**: 即使无闸门的简单情况，流量误差也达15-30%

**原因分析**:
1. 只在入口设置流量边界条件 `hu[0] = Q_target / B`
2. 其他节点的hu通过Preissmann时间推进演化
3. 数值耗散和误差累积导致流量偏离目标值
4. 没有全局流量守恒约束

**可能的解决方案**:
- 方案A: 每步强制所有节点流量相同（但与闸门约束冲突）
- 方案B: 使用投影法或约束优化
- 方案C: 参考原有CanalSolver的处理方式

### 问题2：闸门边界条件策略

**核心矛盾**:
- 稳态流要求：流量处处相同（质量守恒）
- 闸门约束：Q = f(h_up, h_down)
- 这两者必须同时满足

**当前困难**:
1. 直接调整流量 → 破坏守恒
2. 只调整水深 → 流量不收敛
3. 两者都调整 → 需要耦合迭代

**理论上正确的方法**:
- 给定Q和下游h_down，求解上游h_up使得：
  - 闸门公式: Q = Cd * B * e * sqrt(2g * Δh)
  - 上游段满足能量方程/动量方程
- 这是一个耦合的非线性问题

### 问题3：边界条件兼容性

**发现**: 使用随意的下游水深边界条件导致系统无法收敛到正确流量

**测试**:
- h_downstream = 1.5m (随意): 30% 流量误差
- h_downstream = 0.6m (理论均匀流): 仍有15% 误差

**说明**: 边界条件必须与流量相容，但这还不够解决问题

## 当前状态

### 代码文件

| 文件 | 状态 | 说明 |
|------|------|------|
| `solvers/hydrostatic_canal_solver.py` | ⚠️ 部分可用 | 基础功能完成，但流量守恒有问题 |
| `test_hydrostatic_solver.py` | ⚠️ 失败 | 流量误差24% |
| `test_single_canal_with_gate.py` | ✗ 失败 | 多个策略尝试均失败 |

### 良平衡特性保持

**好消息**:
- 静水重构算法本身没有问题
- Ghost cells正确实现
- Audusse源项正确（Phase 1已验证到machine precision）

**坏消息**:
- 时间推进过程中的流量守恒需要额外约束
- 内部边界条件（闸门）的集成策略需重新设计

## 下一步计划

### 短期（调试）

1. **修复基础稳态求解**
   - 研究为什么即使无闸门也有流量误差
   - 检查Preissmann实现是否正确
   - 对比原有CanalSolver的稳态求解方法

2. **简化测试用例**
   - 先确保无闸门情况完全正确（流量误差<1%）
   - 再逐步添加闸门

### 中期（闸门集成）

3. **重新设计闸门策略**
   - 选项A: 参考文献中的内部边界条件处理方法
   - 选项B: 使用原有CanalSolver的松弛迭代方法
   - 选项C: 使用Lagrange乘子法强制约束

4. **单闸门测试**
   - 目标：单渠道+单闸门，流量误差<5%
   - 验证上下游水位差符合闸门公式

### 长期（三渠道集成）

5. **三渠道系统集成**
   - 基于Script 11的三闸门配置
   - 目标精度：<0.5%

6. **性能优化**
   - 加速收敛
   - 减少迭代次数

## 理论参考需求

需要查阅的文献：
1. 内部边界条件（闸门）在浅水方程FVM求解中的标准处理方法
2. 稳态流的流量守恒约束实现
3. Preissmann格式的质量守恒特性

## 经验教训

1. **良平衡≠流量守恒**: 良平衡保证静止状态精确保持，但不自动保证稳态流的流量守恒

2. **边界条件的兼容性很重要**: 随意设置边界条件会导致系统偏离目标

3. **内部边界条件很复杂**: 闸门等内部约束不能简单地"替换"通量，需要系统性的处理

4. **逐步验证很重要**: 应该先确保基础情况完全正确再添加复杂特性

## 结论

**Phase 2 状态**: 进行中（40%完成度）

- ✓ 核心算法集成完成
- ✓ 良平衡特性保持
- ⚠️ 流量守恒存在问题
- ✗ 闸门边界条件未成功集成

**下一步**: 聚焦修复基础稳态求解的流量守恒问题，然后重新设计闸门策略

---

*本文档记录了Phase 2开发中遇到的技术难点，为后续工作提供参考*
