# Week 2基准测试诚实报告

**日期**: 2025-10-27  
**测试内容**: 国际基准算例（MacDonald, Goutal）  
**状态**: ⚠️ **发现重大问题**

---

## 📊 测试结果

### MacDonald Case 1: 均匀流（L=10km）

| 求解器 | 误差 | 结果 | 说明 |
|--------|------|------|------|
| SimpleCorrect | 0.000% | ✅ 完美 | 解析解 |
| Energy | 0.000% | ✅ 完美 | 能量方程 |
| Hybrid | 6.58% | ❌ 失败 | 长渠道累积误差 |
| WellBalanced | 24.23% | ❌ 失败 | 长渠道累积误差 |

### Goutal M1: 壅水曲线（L=10km）

| 求解器 | 误差 | 结果 | 说明 |
|--------|------|------|------|
| SimpleCorrect | 155% | ❌ 失败 | 不支持非均匀流 |
| Hybrid | 156% | ❌ 失败 | 不支持大梯度曲线 |
| Energy | 86% | ❌ 失败 | 未计算出完整曲线 |
| WellBalanced | - | - | 未测试（预计失败） |

**总体通过率**: **2/8 (25%)** ⚠️

---

## 🔍 根本问题分析

### 问题1: 长渠道累积误差

**现象**:
```
MacDonald Case 1（均匀流，10km）:
  
Hybrid:
  - 初始: Q=10.0 m³/s, 误差0.19%
  - 200步: Q=11.16 m³/s, 误差11.57%
  - 2000步: Q=10.82 m³/s, 误差8.23%
  - 未收敛

WellBalanced:
  - 初始: Q=10.0 m³/s, 误差0.38%
  - 100步: Q=6.83 m³/s, 误差-31.68%
  - 900步: Q=-4.71 m³/s, 误差-147%（负流量！）
  - 未收敛
```

**原因**:
1. **数值耗散累积**: 长渠道（10km）vs 短渠道（1km）
2. **收敛困难**: 2000次迭代不够
3. **参数不足**: CFL=0.05仍然太大

**对比Week 1**:
```
Week 1（L=1km）:
  Hybrid: 100% (20/20)
  WellBalanced: 85% (17/20)

Week 2（L=10km）:
  Hybrid: 0% (0/2)
  WellBalanced: 0% (0/2)
```

### 问题2: 非均匀流困难

**Goutal M1壅水曲线**:
- h范围: 2.500m → 12.229m（变化9.7m, 389%）
- 类型: M1降水曲线（上游深，下游浅）

**各求解器表现**:
1. **SimpleCorrect**: 只能计算均匀流（h=0.930m常数）
2. **Hybrid**: 无法处理大梯度
3. **Energy**: 只计算了下游一小段（1.663-2.500m）
4. **WellBalanced**: 预计也会失败

**根本原因**: 所有求解器都不擅长大梯度非均匀流

---

## 📈 Week 1 vs Week 2对比

| 指标 | Week 1 | Week 2 |
|------|--------|--------|
| 场景 | 短渠道（1km）| 长渠道（10km）|
| 类型 | 均匀流为主 | 均匀流+壅水曲线 |
| SimpleCorrect | 100% (51/51) | 50% (1/2) |
| Hybrid | 100% (20/20) | 0% (0/2) |
| Energy | 86% (44/51) | 50% (1/2) |
| WellBalanced | 85% (17/20) | 0% (0/2) |
| **总体** | **93.0%** | **25.0%** |

**结论**: **严重的可扩展性问题**

---

## 💡 问题根源

### 1. 时间推进求解器的固有问题

**Hybrid和WellBalanced**:
```python
# 伪稳态方法: 时间推进到稳态
for t in range(max_iter):
    h_new = h_old + dt * (flux + source)
    Q_new = Q_old + dt * (...)
    
    # 问题: 
    # 1. 每步都有误差 O(dt)
    # 2. N步后累积误差 O(N*dt)
    # 3. 长渠道需要更多步 N↑
    # 4. 累积误差不可控
```

**长渠道效应**:
```
L=1km: 需要~100步收敛, 累积误差小
L=10km: 需要~10000步收敛, 累积误差大10倍
```

### 2. 能量方程的数值不稳定

**Energy求解器**:
```python
# 直接法: 逐断面求解能量方程
H[i] = H[i-1] - hf + bed_gain

# 问题:
# 1. 大梯度时数值震荡
# 2. 可能计算出负水深
# 3. 需要人工阻尼（不稳定）
```

### 3. 简化求解器的局限

**SimpleCorrect**:
- 只支持均匀流（解析解）
- 不支持任何非均匀流
- 功能单一

---

## 🎯 实际可用性评估

### 优秀场景（可放心使用）

**短渠道均匀流**（L < 2km）:
```
✅ SimpleCorrect: 100%（完美）
✅ Hybrid: 100%（Week 1验证）
✅ Energy: 86%（大部分成功）
✅ WellBalanced: 85%（常规场景）
```

### 困难场景（谨慎使用）

**长渠道均匀流**（L > 5km）:
```
✅ SimpleCorrect: 100%（仍然完美）
✅ Energy: 100%（仍然完美）
⚠️ Hybrid: 可能失败（累积误差）
⚠️ WellBalanced: 可能失败（累积误差）
```

### 不支持场景（避免使用）

**大梯度非均匀流**:
```
❌ SimpleCorrect: 不支持
❌ Hybrid: 156%误差
❌ Energy: 86%误差（不完整）
❌ WellBalanced: 预计失败
```

---

## 📊 修正后的总体评估

### Week 1-2综合统计

| 求解器 | Week 1 | Week 2 | 平均 | 实际可用性 |
|--------|--------|--------|------|-----------|
| SimpleCorrect | 100% | 50% | 75% | ★★★★☆ 短渠道完美 |
| Energy | 86% | 50% | 68% | ★★★★☆ 短渠道优秀 |
| Hybrid | 100% | 0% | 50% | ★★★☆☆ 仅短渠道 |
| WellBalanced | 85% | 0% | 43% | ★★★☆☆ 仅短渠道 |

**重要结论**:
```
从Week 1的93%下降到Week 2的25%

原因: Week 1测试过于简单（短渠道）
      Week 2测试更接近实际（长渠道）

实际通过率估计: 40-60%（考虑各种场景）
```

---

## 🎓 经验教训

### 教训1: 早期测试掩盖问题

```
Week 1（短渠道）: 93%通过 → 过度乐观
Week 2（长渠道）: 25%通过 → 暴露问题

结论: 需要更全面的测试覆盖
```

### 教训2: 时间推进方法的局限

```
时间推进求解器（Hybrid, WellBalanced）:
  优点: 通用，支持非恒定流
  缺点: 长渠道累积误差

直接法求解器（Energy, SimpleCorrect）:
  优点: 稳态计算精确
  缺点: 功能有限
```

### 教训3: 没有完美的求解器

```
每个求解器都有局限性:
  - SimpleCorrect: 只支持均匀流
  - Energy: 不支持大梯度
  - Hybrid: 长渠道累积误差
  - WellBalanced: 长渠道累积误差
```

---

## 🚀 改进方向

### 方向1: 多尺度方法

```
短段（<1km）: 使用Hybrid（高精度）
长渠道（>5km）: 分段计算
  - 每段<1km
  - 段间传递边界条件
  - 累积误差控制
```

### 方向2: 自适应CFL

```python
# 根据误差动态调整CFL
if Q_error > 1%:
    CFL *= 0.5  # 降低
else:
    CFL *= 1.1  # 提高

# 目标: 精度和速度平衡
```

### 方向3: 混合方法

```
1. 用SimpleCorrect/Energy计算初始场
2. 用Hybrid/WellBalanced微调
3. 减少迭代次数，控制累积误差
```

---

## ✅ 诚实建议

### 立即可用（有条件）

**推荐场景**:
1. **短渠道**（L < 2km）+ **均匀流** → 所有求解器可用
2. **长渠道**（L > 5km）+ **均匀流** → 使用SimpleCorrect或Energy
3. **临界流** → 使用Energy（Week 1: 100%）

**不推荐场景**:
1. **长渠道** + **大梯度壅水曲线** → 所有求解器都有困难
2. **极端条件** → 需要进一步测试（Week 3）

### 需要改进

**优先级1**: 修复长渠道累积误差
- Hybrid和WellBalanced的可扩展性

**优先级2**: 增强非均匀流支持
- Energy的大梯度处理

**优先级3**: 扩展SimpleCorrect
- 支持简单的渐变流

---

## 🎯 下一步行动

### 选项A: 继续Week 3-4测试

继续测试极端条件和长期稳定性
- 可能发现更多问题
- 通过率可能进一步下降

### 选项B: 先修复再测试

集中修复Week 2暴露的问题:
1. Hybrid长渠道累积误差
2. WellBalanced长渠道累积误差
3. Energy大梯度支持

### 选项C: 接受现状

承认局限性，明确适用范围:
- ✅ 短渠道（<2km）: 93%可用
- ⚠️ 长渠道（>5km）: 部分可用
- ❌ 大梯度壅水曲线: 不支持

**建议**: **选项C + 部分选项B**
- 明确当前适用范围
- 修复最关键的问题（Hybrid长渠道）
- Week 3-4测试可以继续，但降低预期

---

**诚实是最好的策略**  
**Week 1的93%是过度乐观的**  
**实际可用性: 40-60%（取决于场景）**  
**短渠道表现优秀，长渠道有挑战** ⚠️
