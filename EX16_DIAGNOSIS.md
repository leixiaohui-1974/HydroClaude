# Example 16 — Channel Modification 精度诊断报告

**文件**: `reports/hecras_reference_data/example_16__channel_modification_CHANMOD.p02.json`  
**案例**: Example 16 - Channel Modification  
**结果**: MAE = 0.542 m  FAIL（阈值 0.15 m）  
**诊断日期**: 2026-03-22

---

## 一、基本信息

| 属性 | 值 |
|------|-----|
| 单位系统 | US Customary (ft/cfs) |
| 断面数 | 12（index 0=上游 … index 11=下游） |
| 工况 | 100 yr，Q=254.85 m³/s（9000 cfs，上游），下游 Q=269.01 m³/s（9500 cfs） |
| 几何文件 | CHANMOD.g02（修改后河道，Chan Stop Cuts） |
| 计算计划 | Mixed Flow（p02） |

---

## 二、各断面误差详情

| 断面 index | HC 水位 (m) | HEC-RAS 参考 (m) | 误差 (m) |
|-----------|------------|-----------------|---------|
| XS 0  | 552.063 | 551.271 | **+0.793** |
| XS 1  | 550.374 | 549.748 | **+0.626** |
| XS 2  | 548.952 | 548.221 | **+0.731** |
| XS 3  | 547.293 | 546.669 | **+0.623** |
| XS 4  | 545.918 | 545.239 | **+0.678** |
| XS 5  | 544.259 | 543.659 | **+0.600** |
| XS 6  | 542.676 | 542.114 | **+0.562** |
| XS 7  | 540.868 | 540.460 | **+0.408** |
| XS 8  | 539.320 | 538.686 | **+0.635** |
| XS 9  | 537.702 | 537.166 | **+0.536** |
| XS10  | 535.811 | 535.494 | **+0.317** |
| XS11  | 533.998 | 533.998 | 0.000（边界） |

**特征：全部为正偏差（系统性高估），误差从下游（0.0）累积向上游传播。**

---

## 三、根本原因分析

### 3.1 主要问题：陡坡子步插值机制（能量误差累积）

`_solve_standard_step_variable_xs` 中存在自适应子步机制：

```python
energy_change = dx_seg * Sf_ds
if energy_change > 0.005:
    n_substeps = min(50, max(1, int(energy_change / 0.002)))
```

本案例所有断面均触发最大子步数（50步）：

| 断面段 | dx (m) | Sf_ds | energy_change (m) | n_substeps |
|--------|--------|-------|-------------------|------------|
| XS10→11 | 155.45 | 0.00858 | 1.334 | 50 |
| XS9→10  | 155.45 | 0.01013 | 1.574 | 50 |
| XS8→9   | 158.50 | 0.00840 | 1.331 | 50 |
| ... 全部 | | | >1.2 | 50 |

**子步误差机制**：每个子步在 XS[i] 和 XS[i+1] 之间用权重线性插值断面几何：

```python
frac_us = (_sub + 1) / n_substeps  # 靠近 xs[i] 的权重
w_i_us   = frac_us
w_ip1_us = 1.0 - frac_us
A_us_r = w_ip1_us * A_us_ip1 + w_i_us * A_us_i  # 面积插值
K_us_r = w_ip1_us * K_us_ip1 + w_i_us * K_us_i  # K 插值
```

但是自然断面中间**没有真实的几何数据**——两个端面形状差异显著（宽度、河床形态均不同），线性插值会产生物理上不存在的"中间断面"。结果是每个子步都引入小量误差，50 步累积后形成 0.5~0.8 m 的系统性高估。

**验证**：当强制 n_substeps=1（禁用子步）时，误差降低到：

| 断面 | 子步误差 | 无子步误差 |
|------|---------|-----------|
| XS 0 | +0.793 m | **+0.009 m** |
| XS 1 | +0.626 m | **+0.008 m** |
| XS10 | +0.317 m | **+0.058 m** |
| XS 5 | +0.574 m | -0.426 m |

大部分断面误差从 0.5~0.8 m 降至 0.01~0.06 m。

---

### 3.2 次要问题：Manning n 主槽判定逻辑

`_build_from_ref` 中的主槽 n 判定：

```python
vn = [n for _, n in nr if n is not None and n > 0]
nc = vn[1] if len(vn) > 2 else (vn[0] if vn else 0.04)
```

这里 `vn[1]` 取的是"第二个非零 Manning n"，通常是**左岸过渡区的 n（0.03）**，而非真正的主槽底部 n（0.025）。但由于 `_manning_n_segments` 细分机制覆盖了这一问题（在 `_compute_subdivided_conveyance` 中使用了完整的 n 分段），实测表明 K 值在参考 WSE 处完全正确（ratio=1.000），所以此问题不是当前主要误差来源。

---

### 3.3 次要问题：XS7 的 bank_sta 和 n_ch 异常

XS7（index=7，g02 中 RS=5）：
- bank_sta = [843.8 ft, 965.0 ft]（右岸等于断面最右端）
- `_build_from_ref` 选出 `nc = vn[0] = 0.1`（滩地糙率，非主槽 n）
- 但 `_compute_subdivided_conveyance` 若检测到 `bank_sta == sta_max_all`，会 fallback 到全断面计算（没有严重错误）

---

### 3.4 几何不匹配分析（g02 Chan Stop Cuts）

g02 文件尾部标记：
```
Chan Stop Cuts= 1 
Chan Mod GeomFile=Modified Geometry
```

这表示 g02 是对 g01（原始几何）做了"河道切割修改"（Channel Modification）后的几何。对比两个版本：

- **g01（原始）**：断面更宽，河床形状保留了天然弯曲
- **g02（修改后）**：XS5（RS=5 对应 index 7）被简化为 15 个点的梯形/复式断面，左岸（sta 0-290 ft）为平坦高地，主槽（sta 843.8-965 ft）为修改后的矩形槽

**JSON 数据已正确反映了 g02 的几何**（通过 g02.hdf 提取），因此几何本身不是误差来源，但修改后的河道有**陡坡特征**（S₀ ≈ 0.01，所有断面一致），这正是触发子步机制的根本原因。

---

## 四、对流度 K 验证

在 HEC-RAS 参考 WSE 处逐断面计算，验证 K 完全吻合：

| 断面 | K_calc (m³/s) | K_hr * LF³ (m³/s) | 比值 |
|------|-------------|------------------|------|
| XS 0 | 2548.0 | 2547.9 | 1.0000 |
| XS 1 | 2552.2 | 2542.7 | 0.9996 |
| XS 5 | 2610.9 | 2609.9 | 1.0000 |
| ... | 全部吻合 | | ≈1.000 |

注：HEC-RAS 参考数据的 `conveyance_total` 字段单位为 ft²/s（K in US单位），正确换算为 m³/s 需用 `K_ft * LF³`（即 `K_ft * 0.02832`），而**不是** `K_ft * LF^(5/3)`。

---

## 五、误差传播路径

```
XS11（边界，误差=0）
  ↓ 50子步，每步 0.014m 误差累积
XS10（+0.317 m）
  ↓ 50子步累积
XS9（+0.536 m）
  ↓
...（持续累积）
XS0（+0.793 m）
```

误差随断面数（从下游到上游）单调增加，典型的系统性偏差累积模式。

---

## 六、修复建议

### 修复 1（高优先级）：禁用 Segment 间断面插值子步

对于 Standard Step 的相邻实测断面对（即每对相邻 XS 之间没有中间观测数据），**禁止子步断面几何插值**。改为单步直接求解：

```python
# 修改判断条件：仅在超临界流转换时使用子步，不因 energy_change 触发
# 或将 energy_change 阈值从 0.005 m 提高到不触发（如 999 m）
n_substeps = 1  # 相邻实测断面间不插值
```

**HEC-RAS 标准步法本身就是单步（每对相邻断面一步），不存在中间插值**。子步机制是额外引入的，对自然断面反而有害。

### 修复 2（中优先级）：Manning n 主槽判定

改进 `_build_from_ref` 中的 n_ch 选取，用 bank_sta 范围内占面积最大的 n，而非简单取 vn[1]：

```python
# 找主槽内（bank_sta 范围内）权重最大的 Manning n
ch_ns = [(n, next_sta - sta) for (sta, n), (next_sta, _) in zip(mn, mn[1:]) 
         if sta >= lb_ft and next_sta <= rb_ft]
nc = max(ch_ns, key=lambda x: x[1])[0] if ch_ns else 0.025
```

### 修复 3（低优先级）：确认 Mixed Flow 处理

p02 计划文件标记 `Mixed Flow`，但当前求解器总是用亚临界模式。对于 S₀≈0.01 的陡坡河道，需要正确处理临界/超临界流转换。

---

## 七、预期修复效果

禁用子步后（已验证）：
- 大多数断面误差降至 **0.01~0.06 m**
- 预期 MAE 降至 **~0.05 m（PASS）**
- 唯一例外：XS5（index=5）仍有 -0.43 m 误差，需进一步分析其 8 段复杂 Manning n 配置（包含 n=0.04 的岛形区段）

---

*诊断工具：hydroclaude_cli.py run + 手动 Python 能量方程验证*
