# 📋 HydroClaude v2.0 开发计划（6个月详细版）

**计划周期**: 2025-11-15 至 2026-05-15（24周）  
**目标版本**: v2.0 - Production Web Platform  
**团队规模**: 6人核心团队

---

## 🎯 总体目标

将HydroClaude从"核心算法优秀但Web功能薄弱"转变为"完整的生产级Web仿真平台"。

### 关键指标

| 指标 | 当前 | 目标 | 提升 |
|------|------|------|------|
| Web引擎方法数 | 2个 | 15+个 | +650% |
| 集成结构数 | 0种 | 17种 | +∞ |
| 封装求解器数 | 1个 | 30+个 | +2900% |
| 前端完成度 | 5% | 90% | +1700% |
| 测试通过率 | 64% | 90%+ | +41% |
| API端点数 | 3个 | 20+个 | +567% |

---

## 📅 第1-6周：Web引擎完整化（阶段1）

**负责人**: 后端工程师A + 后端工程师B  
**优先级**: ⭐⭐⭐⭐⭐ 最高  
**里程碑**: M1 - Web引擎可用

### Week 1-2: 泵站和闸门集成

#### 任务分解

**后端工程师A**:
```python
# 1. 扩展HydraulicEngine - 泵站功能
class HydraulicEngine:
    def run_pump_simulation(self, task_id, config):
        """
        单泵站仿真
        支持：恒定流量、恒定扬程、变速控制
        """
        pass
    
    def run_canal_with_pump(self, task_id, config):
        """
        明渠+泵站组合
        支持：抽水、排水、灌溉
        """
        pass
```

**后端工程师B**:
```python
# 2. 扩展HydraulicEngine - 闸门功能
class HydraulicEngine:
    def run_gate_simulation(self, task_id, config):
        """
        闸门水力计算
        支持：平板闸门、弧形闸门、翻板闸门
        """
        pass
    
    def run_canal_with_gate(self, task_id, config):
        """
        明渠+闸门组合
        支持：调节流量、控制水位
        """
        pass
```

**交付物**:
- [x] `hydraulic_engine.py` 增加4个方法
- [x] 单元测试4个
- [x] 集成测试2个

### Week 3-4: 堰和水库集成

**后端工程师A**:
```python
# 3. 堰流计算
def run_weir_simulation(self, task_id, config):
    """
    支持6种堰类型：
    - 宽顶堰
    - 薄壁堰
    - V形堰
    - 矩形堰
    - 梯形堰
    - 溢流堰
    """
    pass

def run_canal_with_weir(self, task_id, config):
    """明渠+堰组合"""
    pass
```

**后端工程师B**:
```python
# 4. 水库调度
def run_reservoir_simulation(self, task_id, config):
    """
    水库运行仿真
    支持：洪水演进、调度规则、溢流计算
    """
    pass

def run_reservoir_operation(self, task_id, config):
    """水库优化调度"""
    pass
```

**交付物**:
- [x] `hydraulic_engine.py` 再增加4个方法（共10个）
- [x] 单元测试4个
- [x] 集成测试2个

### Week 5-6: 管网和组合系统

**后端工程师A**:
```python
# 5. 管网仿真
def run_pipe_network(self, task_id, config):
    """
    管道网络水力计算
    求解器：Hardy-Cross法
    """
    pass

def run_pipe_with_valve(self, task_id, config):
    """管道+阀门控制"""
    pass
```

**后端工程师B**:
```python
# 6. 组合系统
def run_integrated_system(self, task_id, config):
    """
    多结构组合仿真
    支持：任意组合水工结构
    """
    pass

def run_canal_network(self, task_id, config):
    """渠道网络仿真"""
    pass
```

**交付物**:
- [x] `hydraulic_engine.py` 最终15+个方法
- [x] 完整单元测试
- [x] 完整集成测试
- [x] M1里程碑达成 ✅

### API路由开发（并行）

**后端工程师A**（Week 1-6）:
```python
# web/backend/api_gateway/routers/structures.py
from fastapi import APIRouter

router = APIRouter()

@router.post("/structures/pump")
async def run_pump_simulation(config: PumpConfig):
    """泵站仿真API"""
    pass

@router.post("/structures/gate")
async def run_gate_simulation(config: GateConfig):
    """闸门仿真API"""
    pass

@router.post("/structures/weir")
async def run_weir_simulation(config: WeirConfig):
    """堰流计算API"""
    pass

@router.post("/structures/valve")
async def run_valve_simulation(config: ValveConfig):
    """阀门控制API"""
    pass
```

**后端工程师B**（Week 1-6）:
```python
# web/backend/api_gateway/routers/reservoir.py
@router.post("/reservoir/routing")
async def reservoir_flood_routing(config: ReservoirConfig):
    """洪水演进API"""
    pass

@router.post("/reservoir/operation")
async def reservoir_operation(config: OperationConfig):
    """水库调度API"""
    pass

# web/backend/api_gateway/routers/network.py
@router.post("/network/pipe")
async def pipe_network_simulation(config: NetworkConfig):
    """管网仿真API"""
    pass

@router.post("/network/canal")
async def canal_network_simulation(config: CanalNetworkConfig):
    """渠道网络API"""
    pass
```

**交付物**:
- [x] 新增3个router文件
- [x] 20+个API端点
- [x] Swagger文档自动生成

---

## 📅 第7-14周：前端Web应用开发（阶段2）

**负责人**: 前端工程师A + 前端工程师B  
**优先级**: ⭐⭐⭐⭐⭐ 最高  
**里程碑**: M2 - 前端发布

### Week 7-8: 项目搭建和基础组件

**前端工程师A**:
```typescript
// 1. 项目结构搭建
web/frontend/src/
├── main.tsx           // 入口文件
├── App.tsx            // 根组件
├── components/        // 组件库
│   ├── Layout/       // 布局组件
│   ├── Header/       // 头部
│   ├── Sidebar/      // 侧边栏
│   └── Footer/       // 底部
├── pages/            // 页面
├── services/         // API服务
├── store/            // 状态管理（Zustand）
└── utils/            // 工具函数

// 2. 基础组件开发
// components/Layout/MainLayout.tsx
export const MainLayout = ({ children }) => {
  return (
    <Layout>
      <Header />
      <Layout>
        <Sider><Sidebar /></Sider>
        <Content>{children}</Content>
      </Layout>
      <Footer />
    </Layout>
  );
};
```

**前端工程师B**:
```typescript
// 3. API服务封装
// services/simulation.ts
import axios from 'axios';

const API_BASE = 'http://localhost:8000';

export const simulationAPI = {
  // 运行明渠仿真
  runCanalSimulation: (config) => {
    return axios.post(`${API_BASE}/simulation/canal`, config);
  },
  
  // 获取仿真状态
  getSimulationStatus: (taskId) => {
    return axios.get(`${API_BASE}/simulation/${taskId}/status`);
  },
  
  // 获取仿真结果
  getSimulationResult: (taskId) => {
    return axios.get(`${API_BASE}/simulation/${taskId}/result`);
  },
};

// 4. 状态管理
// store/simulationStore.ts
import create from 'zustand';

export const useSimulationStore = create((set) => ({
  tasks: [],
  currentTask: null,
  addTask: (task) => set((state) => ({ 
    tasks: [...state.tasks, task] 
  })),
  setCurrentTask: (task) => set({ currentTask: task }),
}));
```

**交付物**:
- [x] 项目搭建完成
- [x] 基础组件库（Layout, Header, Sidebar）
- [x] API服务封装
- [x] 状态管理搭建

### Week 9-10: 仿真配置页面（P0）

**前端工程师A**:
```typescript
// pages/Simulation/ConfigForm.tsx
export const ConfigForm = () => {
  const [config, setConfig] = useState({
    width: 10.0,
    length: 1000.0,
    n_cells: 200,
    manning_n: 0.025,
    slope: 0.001,
    // ... 更多参数
  });
  
  return (
    <Form layout="vertical">
      <Form.Item label="渠道宽度 (m)">
        <InputNumber 
          value={config.width}
          onChange={(v) => setConfig({...config, width: v})}
        />
      </Form.Item>
      
      <Form.Item label="渠道长度 (m)">
        <InputNumber 
          value={config.length}
          onChange={(v) => setConfig({...config, length: v})}
        />
      </Form.Item>
      
      {/* 更多表单项 */}
      
      <Button type="primary" onClick={handleSubmit}>
        开始仿真
      </Button>
    </Form>
  );
};
```

**前端工程师B**:
```typescript
// pages/Simulation/StructureSelector.tsx
export const StructureSelector = ({ onSelect }) => {
  const structures = [
    { id: 'pump', name: '泵站', icon: '🚰' },
    { id: 'gate', name: '闸门', icon: '🚪' },
    { id: 'weir', name: '堰', icon: '🌊' },
    { id: 'valve', name: '阀门', icon: '🔧' },
  ];
  
  return (
    <div className="structure-grid">
      {structures.map((s) => (
        <Card 
          key={s.id}
          onClick={() => onSelect(s)}
          hoverable
        >
          <div className="structure-icon">{s.icon}</div>
          <div className="structure-name">{s.name}</div>
        </Card>
      ))}
    </div>
  );
};
```

**交付物**:
- [x] 仿真配置表单
- [x] 结构选择器
- [x] 初始条件编辑器
- [x] 边界条件编辑器

### Week 11-12: 仿真运行和结果展示（P0）

**前端工程师A**:
```typescript
// pages/Simulation/RunningView.tsx
export const RunningView = ({ taskId }) => {
  const [progress, setProgress] = useState(0);
  const [status, setStatus] = useState('running');
  
  useEffect(() => {
    const timer = setInterval(async () => {
      const res = await simulationAPI.getSimulationStatus(taskId);
      setProgress(res.data.progress);
      setStatus(res.data.status);
      
      if (res.data.status === 'completed') {
        clearInterval(timer);
      }
    }, 1000);
    
    return () => clearInterval(timer);
  }, [taskId]);
  
  return (
    <div>
      <Progress 
        percent={progress} 
        status={status === 'failed' ? 'exception' : 'active'}
      />
      <div className="log-output">
        {/* 实时日志 */}
      </div>
      <Button onClick={handleCancel} danger>
        取消仿真
      </Button>
    </div>
  );
};
```

**前端工程师B**:
```typescript
// pages/Results/ChartView.tsx
import { LineChart, Line, XAxis, YAxis } from 'recharts';

export const ChartView = ({ data }) => {
  return (
    <div>
      <h3>水深-时间曲线</h3>
      <LineChart width={800} height={400} data={data.timeSeries}>
        <XAxis dataKey="time" label="时间 (s)" />
        <YAxis label="水深 (m)" />
        <Line type="monotone" dataKey="h" stroke="#8884d8" />
      </LineChart>
      
      <h3>水面线剖面</h3>
      <LineChart width={800} height={400} data={data.profile}>
        <XAxis dataKey="x" label="距离 (m)" />
        <YAxis label="水位 (m)" />
        <Line type="monotone" dataKey="z" stroke="#82ca9d" />
      </LineChart>
    </div>
  );
};
```

**交付物**:
- [x] 仿真运行页面
- [x] 实时进度显示
- [x] 结果查看页面
- [x] 图表展示组件

### Week 13-14: 案例库和优化（P1）

**前端工程师A**:
```typescript
// pages/Library/CaseLibrary.tsx
export const CaseLibrary = () => {
  const [cases, setCases] = useState([]);
  
  const caseTemplates = [
    {
      id: 1,
      name: '溃坝问题',
      description: '经典溃坝算例',
      preview: '/images/dam_break.png',
      config: { /* 预设配置 */ }
    },
    {
      id: 2,
      name: '水库调度',
      description: '水库优化调度案例',
      preview: '/images/reservoir.png',
      config: { /* 预设配置 */ }
    },
    // 更多案例
  ];
  
  return (
    <Row gutter={[16, 16]}>
      {caseTemplates.map((c) => (
        <Col span={6} key={c.id}>
          <Card
            cover={<img src={c.preview} />}
            actions={[
              <Button onClick={() => loadCase(c)}>加载</Button>,
              <Button onClick={() => runCase(c)}>运行</Button>,
            ]}
          >
            <Card.Meta 
              title={c.name} 
              description={c.description} 
            />
          </Card>
        </Col>
      ))}
    </Row>
  );
};
```

**前端工程师B**:
```typescript
// 性能优化
// 1. 代码分割
const ConfigForm = lazy(() => import('./ConfigForm'));
const ResultView = lazy(() => import('./ResultView'));

// 2. 虚拟滚动（大数据列表）
import { FixedSizeList } from 'react-window';

// 3. 图表懒加载
const ChartView = lazy(() => import('./ChartView'));

// 4. 数据缓存
const queryClient = new QueryClient();
```

**交付物**:
- [x] 案例库页面
- [x] 案例模板10+个
- [x] 性能优化完成
- [x] M2里程碑达成 ✅

---

## 📅 第15-18周：测试完善与质量提升（阶段4）

**负责人**: 测试工程师 + 后端工程师A  
**优先级**: ⭐⭐⭐⭐  
**里程碑**: M3 - 测试达标

### Week 15-16: 修复18个失败测试

**测试工程师**:
```python
# 1. 分析失败原因
python3 web/tests/analyze_specific_failures.py

# 2. 按类别修复

# 类别1: sys.path配置问题（7个）
# 修复方案：统一配置文件
# conftest.py
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent))

# 类别2: 执行超时（5个）
# 修复方案：优化算法或增加超时
@pytest.mark.timeout(300)  # 5分钟
def test_large_scale_simulation():
    pass

# 类别3: 文件路径错误（3个）
# 修复方案：自动创建目录
output_dir = Path("outputs")
output_dir.mkdir(exist_ok=True, parents=True)

# 类别4: 其他错误（3个）
# 逐个debug和修复
```

**后端工程师A**（协助）:
```python
# 修复具体代码问题
# 1. 修复 test_anderson_performance.py
# 2. 修复 test_macdonald4_fine_grid.py
# 3. 修复 test_lake_at_rest_wb.py
```

**交付物**:
- [x] 18个失败测试修复
- [x] 测试通过率 > 80%
- [x] 详细修复文档

### Week 17-18: 新增集成测试

**测试工程师**:
```python
# tests/integration/test_canal_pump_system.py
def test_canal_with_pump_integration():
    """明渠+泵站集成测试"""
    engine = HydraulicEngine()
    
    # 1. 配置明渠+泵站
    config = {
        'canal': {...},
        'pump': {
            'position': 500.0,
            'flow_rate': 10.0,
            'head': 5.0
        }
    }
    
    # 2. 运行仿真
    result = engine.run_canal_with_pump('test_001', config)
    
    # 3. 验证结果
    assert result.status == 'completed'
    assert result.metrics['pump_efficiency'] > 0.8
    # ... 更多断言

# tests/integration/test_reservoir_gate_system.py
def test_reservoir_with_gate_integration():
    """水库+闸门集成测试"""
    # 类似实现
    
# tests/integration/test_canal_network_system.py
def test_canal_network_integration():
    """渠道网络集成测试"""
    # 类似实现

# tests/integration/test_pipe_valve_system.py
def test_pipe_with_valve_integration():
    """管道+阀门集成测试"""
    # 类似实现

# tests/integration/test_hydropower_system.py
def test_hydropower_station_integration():
    """水电站系统集成测试"""
    # 类似实现
```

**后端工程师A**（协助编写测试）:
```python
# tests/e2e/test_web_api_full_workflow.py
import requests

def test_full_simulation_workflow():
    """Web API全流程测试"""
    base_url = "http://localhost:8000"
    
    # 1. 提交仿真
    response = requests.post(f"{base_url}/simulation/canal", json=config)
    assert response.status_code == 200
    task_id = response.json()['task_id']
    
    # 2. 查询状态
    for _ in range(60):  # 最多等待60秒
        response = requests.get(f"{base_url}/simulation/{task_id}/status")
        status = response.json()['status']
        if status == 'completed':
            break
        time.sleep(1)
    
    # 3. 获取结果
    response = requests.get(f"{base_url}/simulation/{task_id}/result")
    assert response.status_code == 200
    result = response.json()
    
    # 4. 验证结果
    assert 'time' in result
    assert 'h' in result
    assert len(result['time']) > 0
```

**交付物**:
- [x] 5个集成测试
- [x] 1个E2E测试
- [x] 测试通过率 > 90%
- [x] M3里程碑达成 ✅

---

## 📅 第19-26周：高级求解器封装（阶段3）

**负责人**: 后端工程师A + 后端工程师B  
**优先级**: ⭐⭐⭐⭐  
**里程碑**: M4 - v2.0发布

### Week 19-20: 第2批求解器（管网）

**后端工程师A**:
```python
# 封装 CanalNetworkSolver
from solvers.canal_network_solver import CanalNetworkSolver

class HydraulicEngine:
    def run_canal_network_solver(self, task_id, config):
        """
        渠道网络求解器
        支持：树状、环状网络
        """
        solver = CanalNetworkSolver(
            nodes=config['nodes'],
            links=config['links'],
            # ... 参数
        )
        
        solver.solve()
        
        return self._package_result(task_id, solver)
```

**后端工程师B**:
```python
# 封装 HardyCrossSolver
from solvers.hardy_cross import HardyCrossSolver

class HydraulicEngine:
    def run_hardy_cross_solver(self, task_id, config):
        """
        Hardy-Cross管网求解器
        适用：复杂管网、环状网络
        """
        # 类似实现
```

**交付物**:
- [x] 封装3个管网求解器
- [x] API端点3个
- [x] 测试3个

### Week 21-22: 第3批求解器（耦合）

**后端工程师A**:
```python
# 封装 CoupledCanalSolver
# 封装 CoupledNetworkSolver
```

**后端工程师B**:
```python
# 封装 HybridSolver
```

**交付物**:
- [x] 封装3个耦合求解器
- [x] API端点3个
- [x] 测试3个

### Week 23-24: 第1批高阶求解器（WENO）

**后端工程师A**:
```python
# 封装 GodunvFVMWENO3V2
# 封装 GodunvFVMWENO5
```

**后端工程师B**:
```python
# 封装 HydrostaticCanalSolver
```

**交付物**:
- [x] 封装3个高阶求解器
- [x] API端点3个
- [x] 测试3个

### Week 25-26: 第4-6批求解器（优化、水质、冰情）

**后端工程师A**:
```python
# 第4批：优化求解器
# - ContinuationSolver
# - NewtonMultiGridSolver

# 第5批：水质求解器
# - DissolvedOxygenSolver
# - NutrientsSolver
# - PhytoplanktonSolver
```

**后端工程师B**:
```python
# 第6批：冰情求解器
# - IceCoverSolver
# - FrazilIceSolver
# - CoupledIceWaterQualitySolver
```

**交付物**:
- [x] 封装10+个专业求解器
- [x] API端点10+个
- [x] 测试10+个
- [x] 总计封装30+求解器 ✅
- [x] M4里程碑达成 ✅

---

## 📅 并行任务：DevOps工程师

### Week 1-26（持续）

**Week 1-4: 基础设施**
```yaml
# 1. Docker化
# Dockerfile
FROM python:3.10-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt --no-cache-dir
COPY . .
EXPOSE 8000
CMD ["uvicorn", "web.backend.api_gateway.main:app", "--host", "0.0.0.0"]

# docker-compose.yml
version: '3.8'
services:
  backend:
    build: .
    ports:
      - "8000:8000"
  frontend:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./web/frontend/dist:/usr/share/nginx/html
```

**Week 5-8: CI/CD**
```yaml
# .github/workflows/ci.yml
name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        run: pytest tests/ --cov=. --cov-report=xml
      - name: Upload coverage
        uses: codecov/codecov-action@v2

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build Docker image
        run: docker build -t hydroclaude:latest .
      - name: Push to registry
        run: docker push hydroclaude:latest
```

**Week 9-26: 监控和优化**
```python
# 1. 日志系统
import logging
logging.basicConfig(
    format='%(asctime)s [%(levelname)s] %(message)s',
    level=logging.INFO,
    handlers=[
        logging.FileHandler("app.log"),
        logging.StreamHandler()
    ]
)

# 2. 性能监控
from prometheus_client import Counter, Histogram

simulation_counter = Counter('simulations_total', 'Total simulations')
simulation_duration = Histogram('simulation_duration_seconds', 'Simulation duration')

# 3. 健康检查
@app.get("/health")
async def health_check():
    return {"status": "healthy"}
```

**交付物**:
- [x] Docker镜像
- [x] CI/CD流水线
- [x] 日志系统
- [x] 监控系统
- [x] 部署文档

---

## 📊 交付清单

### 代码交付

| 模块 | 文件数 | 代码行数 | 测试覆盖率 |
|------|--------|----------|------------|
| 后端引擎 | +15个方法 | +2000行 | 90% |
| API路由 | +20个端点 | +1500行 | 85% |
| 前端应用 | +50个组件 | +5000行 | 80% |
| 求解器封装 | +30个 | +3000行 | 90% |
| 测试代码 | +50个测试 | +2000行 | - |
| **总计** | **+165个** | **+13500行** | **86%** |

### 功能交付

**Web引擎方法（15个）**:
1. run_canal_simulation ✅
2. run_pump_simulation 🆕
3. run_canal_with_pump 🆕
4. run_gate_simulation 🆕
5. run_canal_with_gate 🆕
6. run_weir_simulation 🆕
7. run_canal_with_weir 🆕
8. run_reservoir_simulation 🆕
9. run_reservoir_operation 🆕
10. run_pipe_network 🆕
11. run_pipe_with_valve 🆕
12. run_integrated_system 🆕
13. run_canal_network 🆕
14. run_canal_network_solver 🆕
15. run_hardy_cross_solver 🆕

**API端点（20+个）**:
- /simulation/canal ✅
- /structures/pump 🆕
- /structures/gate 🆕
- /structures/weir 🆕
- /structures/valve 🆕
- /reservoir/routing 🆕
- /reservoir/operation 🆕
- /network/pipe 🆕
- /network/canal 🆕
- ... 更多

**前端页面（10+个）**:
- 首页 🆕
- 仿真配置 🆕
- 仿真运行 🆕
- 结果查看 🆕
- 案例库 🆕
- ... 更多

**求解器封装（30+个）**:
- GodunvFVMSolver ✅
- HydrostaticCanalSolver 🆕
- CanalNetworkSolver 🆕
- HardyCrossSolver 🆕
- ... 更多26个

### 文档交付

- [ ] API文档（自动生成）
- [ ] 用户手册
- [ ] 开发者指南
- [ ] 部署文档
- [ ] 测试报告

---

## 🎯 成功验收标准

### 功能验收

- [ ] Web引擎15+个方法全部可用
- [ ] API 20+个端点全部正常
- [ ] 前端10+个页面全部可访问
- [ ] 求解器30+个全部封装
- [ ] 测试通过率 > 90%

### 性能验收

- [ ] API响应时间 < 500ms
- [ ] 前端首屏加载 < 2s
- [ ] 仿真速度提升10x（Numba）
- [ ] 并发支持 > 10用户

### 质量验收

- [ ] 代码覆盖率 > 85%
- [ ] 无严重Bug
- [ ] 文档完整
- [ ] 通过用户测试

---

## 📞 项目管理

### 周会制度

**每周一上午**:
- 回顾上周进度
- 计划本周任务
- 识别风险和阻塞

**每周五下午**:
- 演示本周成果
- 提交代码审查
- 更新项目文档

### 里程碑评审

**M1 (Week 6)**: Web引擎评审
**M2 (Week 14)**: 前端发布评审
**M3 (Week 18)**: 测试达标评审
**M4 (Week 26)**: v2.0发布评审

### 风险管理

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 求解器封装困难 | 中 | 高 | 技术预研、专家咨询 |
| 前端性能不足 | 低 | 中 | 性能测试、优化方案 |
| 测试覆盖不足 | 低 | 中 | 自动化测试、Code Review |
| 进度延期 | 中 | 高 | 周会跟踪、及时调整 |

---

**计划制定人**: AI 项目经理  
**审核人**: 项目技术负责人  
**批准日期**: 2025-11-15

🎯 **Let's make HydroClaude great!**
