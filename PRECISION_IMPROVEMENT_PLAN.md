# 基础算法库精度提升计划
## 目标：误差从10^-2降至10^-4（降低2个数量级）

---

## 📊 当前状态分析

### 问题诊断
```
脚本11场景1结果：
- 目标流量：10.0 m³/s
- 实际误差：1.35% (1.35×10^-2)
- 误差表现：闸门处流量偏差约0.4-0.5 m³/s

误差来源：
1. ✗ 收敛标准不够严格（只检查全局平均）
2. ✗ 迭代次数可能不足
3. ✗ 数值耗散（theta-omega松弛参数）
4. ✗ 闸门处网格分辨率不足
5. ✗ 时间步长太大（dt=1.0s）
```

---

## 🎯 改进方案设计

### 方案1：多层次收敛判据（最关键）⭐⭐⭐

**当前问题：**
```python
# 只检查全局平均误差
Q_avg = np.mean(self.solver.Q[1:-1])
Q_error = abs(Q_avg - Q_target) / Q_target
if Q_error < convergence_tol:  # 0.001
    converged = True
```

**改进方案：**
```python
收敛判据层次：
Level 1 - 全局守恒：
  |Q_avg - Q_target| / Q_target < tol_global (10^-4)

Level 2 - 局部守恒：
  max(|Q[i] - Q[i+1]|) / Q_target < tol_local (10^-4)
  
Level 3 - 结构守恒：
  |Q_structure - Q_target| / Q_target < tol_structure (10^-4)
  
Level 4 - 时间稳定性：
  max(|Q[t] - Q[t-dt]|) / Q[t] < tol_temporal (10^-5)

同时满足所有层次才算收敛！
```

### 方案2：自适应时间步进 ⭐⭐

**当前问题：**
```python
dt = 1.0  # 固定时间步长
```

**改进方案：**
```python
自适应策略：
- 初始阶段：dt = 1.0s （快速接近稳态）
- 中期阶段：dt = 0.5s （误差<1%时）
- 精化阶段：dt = 0.1s （误差<0.1%时）
- 最终阶段：dt = 0.01s（误差<0.01%时）

CFL数控制：
CFL = v_max * dt / dx < 0.5 （保证稳定性）
```

### 方案3：高阶通量限制器 ⭐⭐

**当前问题：**
```python
# 一阶迎风格式在闸门处产生数值耗散
```

**改进方案：**
```python
在结构位置使用：
1. TVD (Total Variation Diminishing) 限制器
2. MUSCL (Monotonic Upstream-centered Scheme)
3. 局部网格加密（闸门附近2倍分辨率）
```

### 方案4：增强松弛控制 ⭐

**当前问题：**
```python
omega = 0.8  # 固定松弛因子
theta = 0.6  # 固定权重
```

**改进方案：**
```python
自适应松弛：
- 初始：omega = 0.9 （快速收敛）
- 中期：omega = 0.7 （稳定）
- 精化：omega = 0.5 （高精度）

动态调整策略：
if residual_decreasing:
    omega *= 1.05  # 加速
else:
    omega *= 0.95  # 减速
```

### 方案5：残差监控系统 ⭐

**新增功能：**
```python
多种残差指标：
1. L1范数：sum(|Q[i] - Q_target|) / n
2. L2范数：sqrt(sum((Q[i] - Q_target)^2) / n)
3. L∞范数：max(|Q[i] - Q_target|)
4. 结构残差：|Q_gate - Q_target|

停止准则：
所有残差 < 10^-4
```

---

## 🔧 实施计划

### Phase 1: 核心算法增强
```
1. single_canal_solver.py
   - 新增 solve_steady_state_high_precision()
   - 多层次收敛判据
   - 自适应时间步进
   
2. canal_solver.py  
   - 增强 step_preissmann() 精度
   - 添加局部残差计算
   - 实现TVD限制器
```

### Phase 2: 测试验证
```
1. 创建 test_precision_improvement.py
   - 单闸门测试用例
   - 三闸门串联测试
   - 混合结构测试
   
2. 验证指标
   - 全局误差 < 0.01%
   - 局部误差 < 0.01%
   - 结构误差 < 0.01%
```

### Phase 3: 应用到脚本11
```
1. 修改 11_advanced_structures.py
   - 使用高精度求解器
   - 增加收敛监控输出
   
2. 对比验证
   - 旧版：误差 1.35%
   - 新版：误差 < 0.01%
```

---

## 📈 预期效果

### 误差降低目标
```
场景1 - 三闸门：
  当前：1.35% → 目标：<0.01% (降低135倍)

场景2 - 混合结构：
  当前：0.027% → 目标：<0.01% (保持优势)

场景3 - 时变闸门：
  当前：5.56% → 目标：<0.05% (降低100倍)
```

### 计算成本
```
预计迭代次数增加：2-3倍
预计计算时间增加：2-4倍
但精度提升：100倍以上

性价比：优秀！
```

---

## ⚠️ 风险控制

### 数值稳定性
```
- 小时间步可能导致累积误差
- 解决：使用Kahan求和算法
- 解决：定期重新正交化
```

### 收敛速度
```
- 严格收敛标准可能很慢
- 解决：粗精度二阶段求解
- 解决：外推加速
```

---

## 📝 实施检查清单

- [ ] 实现多层次收敛判据
- [ ] 实现自适应时间步进
- [ ] 增强结构处数值处理
- [ ] 创建精度测试套件
- [ ] 验证误差<0.01%
- [ ] 更新文档
- [ ] 提交新分支

