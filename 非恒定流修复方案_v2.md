# 非恒定流修复方案 v2.0

**日期**: 2025-10-26  
**目标**: 彻底解决泵站在非恒定流中的流量守恒问题

---

## 🎯 核心策略

### 关键洞察

**问题**：泵站掩码在非恒定流中阻止节点更新 → 流量累积  
**错误尝试**：完全禁用掩码 → 数值发散

**正确方案**：
1. 让泵站节点参与Preissmann更新（获取物理合理的初值）
2. 更新后立即应用泵站跳跃条件（纠正为正确状态）
3. 确保跳跃条件本身保持流量守恒

---

## 🔧 实施方案

### 步骤1: 修改泵站跳跃条件（关键）

当前`_apply_pump_head_jump()`实现：
```python
def _apply_pump_head_jump(self):
    h_upstream = self.h[idx - 1]
    hu_upstream = self.hu[idx - 1]
    
    # 施加跳跃
    h_downstream = h_upstream + rated_head
    hu_downstream = hu_upstream  # 流量连续
    
    self.h[idx + 1] = h_downstream
    self.hu[idx + 1] = hu_downstream
    self.h[idx] = 0.5 * (h_upstream + h_downstream)
    self.hu[idx] = hu_upstream
```

**问题**：这会覆盖Preissmann更新的结果，可能破坏局部守恒

**改进**：仅调整水深，保持流量
```python
def _apply_pump_head_jump(self):
    # 获取当前流量（来自Preissmann更新）
    Q_upstream = self.hu[idx-1] * self.B
    Q_pump = self.hu[idx] * self.B
    Q_downstream = self.hu[idx+1] * self.B
    
    # 平均流量（保持守恒）
    Q_avg = (Q_upstream + Q_pump + Q_downstream) / 3.0
    
    # 施加水深跳跃，但保持流量
    h_upstream = self.h[idx - 1]
    h_downstream = h_upstream + rated_head
    
    self.h[idx + 1] = h_downstream
    self.h[idx] = 0.5 * (h_upstream + h_downstream)
    
    # 保持流量守恒
    self.hu[idx-1] = Q_avg / self.B
    self.hu[idx] = Q_avg / self.B
    self.hu[idx+1] = Q_avg / self.B
```

### 步骤2: 在非恒定流中使用改进的跳跃条件

```python
def solve_transient(...):
    # Preissmann步（不使用掩码）
    h_new, hu_new = self.step_preissmann(dt, enforce_bc=True, 
                                         Q_in=Q_in, h_out=h_out,
                                         use_pump_mask=False)
    
    self.h = h_new
    self.hu = hu_new
    
    # 应用改进的泵站跳跃（保持流量守恒）
    self._apply_pump_head_jump()
    
    # 应用闸门边界条件
    if self.structure_indices:
        self._apply_internal_bc(...)
```

---

## 📋 实施步骤

1. ✅ 保持use_pump_mask参数（已完成）
2. 🔄 改进`_apply_pump_head_jump()`保持流量守恒
3. 🔄 在非恒定流中使用改进方法
4. ✓ 验证各种系统
5. ✓ 运行综合测试

---

## 🧪 预期效果

| 系统 | 当前 | 预期 |
|-----|------|------|
| 无结构物 | ✅ 0.21% | ✅ < 1% |
| 单闸门 | ✅ 0.07% | ✅ < 1% |
| 单泵站 | ❌ 发散 | ✅ < 5% |
| 多结构物 | ❌ > 100% | ✅ < 5% |

---

*方案生成时间: 2025-10-26*  
*版本: v2.0 - 流量守恒型跳跃条件*
