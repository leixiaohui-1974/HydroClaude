# 算法正确性优先策略
# Algorithm Correctness First Strategy

**创建时间**: 2025-11-13 11:20
**核心原则**: 100%通过率 + 结果正确性验证

---

## 🎯 **核心目标**

```
目标1: 后台算法测试通过率 = 100%
目标2: 所有结果必须正确（流量守恒、物理合理性）
目标3: 验证比通过率更重要
```

## ⚠️ **当前问题分析**

### 现状
```
通过率: 18.9% (102/541)
失败率: 81.1% (439/541)
```

### 问题分类

#### 类型1: 真实的算法/实现问题 ❌
```
症状: 数值发散、质量不守恒、非物理结果
原因: 算法bug、数值格式错误、边界条件处理错误
解决: 必须修复算法，不能调参数掩盖
```

#### 类型2: 测试配置不合理 ⚙️
```
症状: 过于激进的CFL、网格分辨率不足
原因: 测试参数设置超出数值方法的稳定性范围
解决: 可以调整配置，但要说明原因
```

#### 类型3: 测试案例本身有问题 📝
```
症状: 物理参数不合理、预期结果错误
原因: 测试设计错误
解决: 修正测试案例
```

---

## 📋 **新的诊断流程**

### Phase 1: 验证基础库（最重要）

#### Step 1.1: 测试核心求解器
```bash
# 测试HydrostaticCanalSolver的基础功能
python tests/core_functionality_verification_v2.py
```

**验证指标**:
- ✅ 流量守恒误差 < 0.01%
- ✅ 稳态收敛
- ✅ 均匀流与理论值匹配

#### Step 1.2: 测试Lake at Rest
```bash
# Well-balanced特性验证
python tests/test_lake_at_rest_wb.py
```

**验证指标**:
- ✅ 水深保持常数
- ✅ 流速 = 0
- ✅ 数值误差 < 1e-10

#### Step 1.3: 测试简单结构
```bash
# 单闸门流动
python examples/example_01_canal_flow/scripts/07_sluice_gate_flow_v2.py
```

**验证指标**:
- ✅ 闸门上下游流量一致
- ✅ 水跃位置合理
- ✅ 稳态误差 < 0.01%

### Phase 2: 分类失败案例

创建脚本分析每个失败案例的**失败原因**：

```python
失败原因分类:
1. 数值发散 (NaN/Inf) → 算法问题
2. 质量不守恒 → 算法问题
3. 负水深 → 算法问题
4. 超时 → 可能是配置问题
5. Import错误 → 代码问题
6. 其他运行时错误 → 需要具体分析
```

### Phase 3: 逐个修复

**优先级**:
```
P0: 基础库bug（影响所有测试）
P1: 常用功能bug（影响多个测试）
P2: 特定场景bug（影响个别测试）
P3: 配置优化（不是真正的bug）
```

---

## 🔧 **立即行动计划**

### Step 1: 验证基础功能（5分钟）
```bash
# 运行3个核心测试
python tests/core_functionality_verification_v2.py
python tests/test_lake_at_rest_wb.py
python examples/example_01_canal_flow/scripts/07_sluice_gate_flow_v2.py
```

**如果这3个都失败** → 基础库有严重问题，必须先修复
**如果这3个都通过** → 问题可能在复杂场景或配置

### Step 2: 详细诊断失败案例（10分钟）
```bash
# 创建失败诊断脚本
python diagnose_failure_root_causes.py
```

输出:
```
失败原因统计:
- 数值发散: X 个
- 质量不守恒: Y 个
- Import错误: Z 个
- ...
```

### Step 3: 修复策略决策（根据诊断结果）

**情况A: 基础库有bug**
```
行动: 修复求解器代码
预期: 大幅提升通过率 (50%+)
```

**情况B: 配置问题为主**
```
行动: 优化测试配置
预期: 中度提升通过率 (10-20%)
```

**情况C: 测试案例问题**
```
行动: 修正测试案例
预期: 小幅提升通过率 (5-10%)
```

---

## 🎯 **验证标准（严格）**

### 流量守恒验证
```python
# 每个测试都要验证
Q_in = ...
Q_out = ...
error = abs(Q_out - Q_in) / Q_in * 100

assert error < 0.01, f"流量误差 {error:.4f}% 超标"
```

### 物理合理性验证
```python
# 水深必须非负
assert np.all(h >= 0), "出现负水深"

# Froude数合理
Fr = u / np.sqrt(g * h)
assert np.all(Fr < 10), "Froude数异常"

# 能量守恒（稳态）
E = h + u**2/(2*g) + z
assert np.std(E) < 0.01, "能量不守恒"
```

### 收敛性验证
```python
# 稳态必须收敛
assert converged, "稳态求解未收敛"
assert iterations < max_iter, "达到最大迭代次数"
```

---

## 📊 **预期成果**

### 阶段性目标
```
当前: 18.9% 通过率
目标1: 基础功能 100% 通过 (10个核心测试)
目标2: 简单场景 100% 通过 (50个测试)
目标3: 复杂场景 90%+ 通过 (所有测试)
最终: 100% 通过率 + 所有结果验证通过
```

### 时间估算
```
基础验证: 5分钟
失败诊断: 10分钟
bug修复: 根据问题严重程度 (30分钟 - 2小时)
完整测试: 40分钟
总计: 约2-3小时
```

---

## 🚀 **开始执行**

让我先运行基础验证，确定问题的根本原因...

