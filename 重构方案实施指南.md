# 串联闸泵群明渠模型重构实施指南（快速版）

**日期**: 2025-10-27  
**目标**: 3-4周内将精度从2.32%提升至0.5%以内

---

## 🎯 一页总结

### 问题根源（5个致命缺陷）

```
❌ 缺陷1: 非均匀网格算法错误 → 贡献 1.5-2.0% 误差
   └─ 计算时使用平均dx而非局部dx，网格加密无效

❌ 缺陷2: 守恒性被人为破坏 → 贡献 0.5-1.0% 误差  
   └─ 闸门边界条件的"平滑"操作破坏质量守恒

❌ 缺陷3: 空间滤波器抹除物理间断 → 贡献 0.5-1.0% 误差
   └─ Savitzky-Golay滤波把真实的闸门收缩平滑掉

❌ 缺陷4: 算法框架不适合稳态 → 结构性限制
   └─ 用瞬态方法（FDM+Preissmann）求稳态，精度极限2.32%

❌ 缺陷5: 良平衡性缺失 → 稳态无法精确保持
   └─ 无法达到机器精度的静水平衡
```

### 解决方案（推荐方案A）

```
✅ 修复1: 正确实现非均匀网格导数 [2-3天] → -1.5%误差
✅ 修复2: 删除守恒破坏代码 [1天] → -0.8%误差  
✅ 修复3: 条件性空间滤波 [2天] → -0.5%误差
✅ 修复4: 引入静水重构法 [5-7天] → -0.4%误差
✅ 修复5: （可选）能量方程法 [3-5天] → 进一步优化

总时间: 3-4周
预期精度: 0.3-0.5%（达标）
```

---

## 📅 3周实施计划

### Week 1: 快速修复（关键周）

#### Day 1-2: 修复非均匀网格 ⭐⭐⭐⭐⭐

**任务**: 修改所有导数计算使用局部dx

**代码修改**:
```python
# 文件: solvers/canal_solver.py

# 之前（错误）:
dQ_dx = (Q[i+1] - Q[i-1]) / (2 * self.dx)

# 之后（正确）:
dx_left = self.dx_local[i-1]
dx_right = self.dx_local[i]
dQ_dx = (Q[i+1] - Q[i-1]) / (dx_left + dx_right)
```

**需要修改的方法**:
- `step_explicit()` - 约8处
- `step_preissmann()` - 约4处  
- `step_hll()` - 约6处

**测试标准**:
- 网格加密测试：dx=1m时误差应<1%（不是2.9%）
- 质量守恒：ΔQ < 0.01%

**预期**: 误差从2.32% → 0.8-1.2%

---

#### Day 3: 移除守恒破坏 ⭐⭐⭐⭐

**任务**: 删除闸门边界条件的"平滑"代码

**代码修改**:
```python
# 文件: solvers/canal_solver.py, lines 191-194

# 删除这4行:
# if idx > 1:
#     self.Q[idx - 1] = 0.5 * (self.Q[idx - 2] + Q_gate_new)  
# if idx < self.nx - 2:
#     self.Q[idx + 1] = 0.5 * (Q_gate_new + self.Q[idx + 2])
```

**测试标准**:
- 质量守恒：∫(Q_in - Q_out)dt ≈ 0（机器精度）
- 检查是否有数值振荡

**如有振荡**: 微调theta参数（0.6 → 0.65）

**预期**: 进一步降低误差0.5-1.0%

---

#### Day 4-5: 测试和验证

**测试场景**:
1. 单闸门（example 07）
2. 三闸门串联（当前测试案例）
3. 串联闸泵群（目标案例）

**验证指标**:
- 流量误差 < 1.5%（Week 1目标）
- 质量守恒 < 0.01%
- 收敛稳定性 100%

---

### Week 2-3: 良平衡改进

#### Day 6-7: 研读理论 📚

**必读论文**:
1. Audusse et al. (2004) "Hydrostatic Reconstruction" - SIAM JSCC
   - 重点：Section 3 静水重构算法
   - 重点：Section 4 良平衡性质证明

**理解要点**:
- 什么是良平衡性质（well-balanced property）
- 为什么标准FV不保持稳态
- 静水重构如何解决问题

---

#### Day 8-12: 实现静水重构 ⭐⭐⭐⭐⭐

**新增代码结构**:
```python
# 文件: solvers/hydrostatic_reconstruction.py（新建）

class HydrostaticReconstruction:
    def reconstruct(self, h_L, h_R, z_L, z_R):
        """静水重构算法"""
        z_interface = max(z_L, z_R)
        eta_L = h_L + z_L  # 水位
        eta_R = h_R + z_R
        
        h_L_star = max(0, eta_L - z_interface)
        h_R_star = max(0, eta_R - z_interface)
        
        return h_L_star, h_R_star
    
    def compute_balanced_source(self, ...):
        """良平衡源项"""
        ...
```

**集成到求解器**:
```python
# 文件: solvers/hydrostatic_canal_solver.py

def compute_fluxes_hll(self):
    for i in range(1, self.nx-1):
        # 静水重构
        h_L, h_R = self.hydro_recon.reconstruct(
            self.h[i], self.h[i+1], self.z[i], self.z[i+1]
        )
        
        # 使用重构后的状态计算通量
        F = self.hll_flux(h_L, h_R, ...)
        
        # 良平衡源项修正
        S = self.hydro_recon.compute_balanced_source(...)
```

**测试标准**:
- 静水测试：静止水体（Q=0）误差 < 1e-10
- 稳态测试：均匀流误差 < 0.1%
- 闸门测试：流量误差 < 0.5%

**预期**: 误差降至 0.3-0.5%

---

#### Day 13-14: 条件性滤波 ⭐⭐⭐

**任务**: 仅对远离结构的区域滤波

**代码修改**:
```python
def apply_spatial_filter(self, field):
    """条件性滤波：保护结构物附近区域"""
    filtered = field.copy()
    
    for i in range(self.nx):
        # 计算到最近结构物的距离
        min_dist = min(abs(self.x[i] - s.position) 
                      for s in self.structures)
        
        # 仅当距离>10*dx时滤波
        if min_dist > 10 * self.dx:
            window = field[max(0,i-2):min(self.nx,i+3)]
            filtered[i] = savgol_filter(window, ...)[2]
    
    return filtered
```

**测试标准**:
- 闸门附近（±5dx）：不滤波
- 远离结构（>10dx）：正常滤波
- 无数值振荡

---

#### Day 15: 综合测试

**所有场景测试**:
- [ ] 单闸门
- [ ] 双闸门  
- [ ] 三闸门
- [ ] 单泵站
- [ ] 串联闸泵群

**性能指标**:
- 流量误差 < 0.5% ✅  
- 质量守恒 < 0.01% ✅
- 泵站扬程误差 < 1% ✅
- 收敛稳定 100% ✅

---

### Week 4（可选）: 能量方程法

**如果Week 3达标**: 跳过，进入验证和文档阶段

**如果仍未达标**: 实现HEC-RAS风格的能量方程稳态求解器

**新模块**:
```python
# 文件: solvers/energy_equation_solver.py（新建）

class SteadyEnergyEquationSolver:
    def solve(self, Q_target, h_downstream):
        """
        HEC-RAS风格：从下游向上游求解
        
        能量方程: E_up = E_down + h_friction + h_slope ± h_structure
        """
        ...
```

---

## 🧪 测试用例清单

### 基础测试（必须通过）

```python
# test_basic_flow.py

def test_uniform_flow():
    """均匀流测试"""
    solver = HydrostaticCanalSolver(...)
    result = solver.solve_steady_state(Q=10.0)
    
    # 理论水深（Manning公式）
    h_theory = compute_uniform_depth(Q=10.0, B=10, S0=0.001, n=0.025)
    
    # 数值水深（中段平均）
    h_numeric = np.mean(result['h'][40:60])
    
    error = abs(h_numeric - h_theory) / h_theory * 100
    assert error < 0.5%, f"均匀流误差{error:.2f}%超标"

def test_mass_conservation():
    """质量守恒测试"""
    result = solver.solve_steady_state(Q=10.0)
    
    Q_in = result['Q'][0]
    Q_out = result['Q'][-1]
    Q_avg = np.mean(result['Q'][20:80])
    
    error = abs(Q_avg - 10.0) / 10.0 * 100
    assert error < 0.01%, f"质量守恒误差{error:.4f}%超标"

def test_hydrostatic_balance():
    """静水平衡测试（良平衡性）"""
    solver = HydrostaticCanalSolver(...)
    solver.h[:] = 5.0  # 静止水体
    solver.Q[:] = 0.0
    
    # 演化10000步
    for _ in range(10000):
        solver.step(dt=1.0)
    
    # 水深应保持不变（机器精度）
    h_change = np.max(np.abs(solver.h - 5.0))
    assert h_change < 1e-10, f"静水平衡误差{h_change:.2e}超标"
```

---

### 结构物测试（核心）

```python
def test_single_gate():
    """单闸门测试"""
    solver = create_solver_with_gate(position=5000, opening=3.0)
    result = solver.solve_steady_state(Q=10.0)
    
    # 闸门流量计算（理论）
    gate = solver.structures[0]
    Q_theory = gate.calculate_discharge(h_up=result['h'][49], 
                                        h_down=result['h'][51])
    
    error = abs(result['Q'][50] - Q_theory) / Q_theory * 100
    assert error < 1.0%, f"闸门流量误差{error:.2f}%超标"

def test_pump_station():
    """泵站测试"""
    solver = create_solver_with_pump(position=5000, rated_head=5.0)
    result = solver.solve_steady_state(Q=10.0)
    
    # 泵站扬程测量
    h_up = result['h'][49]
    h_down = result['h'][51]
    H_measured = h_down - h_up
    H_rated = 5.0
    
    error = abs(H_measured - H_rated) / H_rated * 100
    assert error < 1.0%, f"泵站扬程误差{error:.2f}%超标"

def test_gate_pump_cascade():
    """串联闸泵群测试（最终目标）"""
    solver = create_gate_pump_cascade_system()
    result = solver.solve_steady_state(Q=10.0)
    
    # 多项检查
    assert_flow_conservation(result, tolerance=0.5%)
    assert_energy_balance(result, tolerance=3%)
    assert_pump_head(result, tolerance=1%)
    assert_gate_flow(result, tolerance=1%)
```

---

## 📊 性能监控仪表板

### 实时监控指标

```python
class PerformanceMonitor:
    def __init__(self):
        self.metrics = {
            'flow_error': [],
            'mass_conservation': [],
            'energy_balance': [],
            'iterations': [],
            'wall_time': []
        }
    
    def checkpoint(self, iteration, result):
        """每100步记录一次"""
        if iteration % 100 == 0:
            # 流量误差
            Q_avg = np.mean(result['Q'])
            Q_target = 10.0
            flow_error = abs(Q_avg - Q_target) / Q_target * 100
            
            # 质量守恒
            Q_std = np.std(result['Q'])
            mass_error = Q_std / Q_target * 100
            
            self.metrics['flow_error'].append(flow_error)
            self.metrics['mass_conservation'].append(mass_error)
            
            # 打印进度
            print(f"Iter {iteration}: "
                  f"Flow error={flow_error:.3f}%, "
                  f"Mass error={mass_error:.4f}%")
```

### 收敛曲线可视化

```python
def plot_convergence(monitor):
    """绘制收敛曲线"""
    fig, axes = plt.subplots(2, 2, figsize=(12, 8))
    
    # 流量误差
    axes[0,0].semilogy(monitor.metrics['flow_error'])
    axes[0,0].axhline(y=0.5, color='r', linestyle='--', label='目标')
    axes[0,0].set_ylabel('流量误差 (%)')
    axes[0,0].legend()
    
    # 质量守恒
    axes[0,1].semilogy(monitor.metrics['mass_conservation'])
    axes[0,1].axhline(y=0.01, color='r', linestyle='--', label='目标')
    axes[0,1].set_ylabel('质量守恒误差 (%)')
    
    # 迭代次数
    axes[1,0].plot(monitor.metrics['iterations'])
    axes[1,0].set_ylabel('迭代次数')
    
    # 计算时间
    axes[1,1].plot(monitor.metrics['wall_time'])
    axes[1,1].set_ylabel('计算时间 (s)')
    
    plt.tight_layout()
    plt.savefig('convergence_history.png', dpi=150)
```

---

## 🎯 成功判定标准

### Week 1 结束（Day 5）

✅ **必须达成**:
- 流量误差 < 1.5%
- 质量守恒 < 0.1%
- 无崩溃、发散

❌ **如未达成**: 深入调试Day 1-3的修复

---

### Week 2-3 结束（Day 15）

✅ **必须达成** (Tier 1):
- 流量误差 < 0.5% ← **核心目标**
- 质量守恒 < 0.01%
- 泵站扬程误差 < 1%
- 收敛稳定 100%

✅ **应该达成** (Tier 2):
- 流量误差 < 0.3%
- 能量守恒 < 3%
- 迭代次数 < 1000

✅ **最好达成** (Tier 3):
- 流量误差 < 0.1%
- 静水平衡 < 1e-10

❌ **如未达成Tier 1**: 考虑Week 4能量方程法

---

## 🚨 常见问题和解决方案

### Q1: 修复后出现数值振荡

**症状**: 水深/流量出现高频振荡

**原因**: 移除平滑后，数值格式稳定性降低

**解决**:
1. 微调theta: 0.6 → 0.65 → 0.7
2. 微调omega: 0.95 → 0.9 → 0.85
3. 减小时间步长: dt/2
4. 检查CFL条件: CFL < 0.5

---

### Q2: 静水重构后精度没提升

**原因**: 实现错误或源项不平衡

**检查清单**:
- [ ] 静水测试通过？（h不变）
- [ ] 源项离散化是否与通量匹配？
- [ ] 床面高程z是否正确传递？
- [ ] 干湿界面处理是否正确？

**调试方法**:
```python
# 打印调试信息
print(f"h_L={h_L}, h_R={h_R}, z_L={z_L}, z_R={z_R}")
print(f"h_L_star={h_L_star}, h_R_star={h_R_star}")
print(f"Flux={F}, Source={S}, Balance={F-S}")
```

---

### Q3: 网格加密仍无效

**检查**: 是否所有导数都修改了？

**验证方法**:
```python
# 在代码中搜索
grep -n "/ (2 * self.dx)" solvers/*.py

# 应该没有输出（都已修改）
```

---

## 📚 快速参考

### 关键参数

| 参数 | 当前值 | 调整范围 | 作用 |
|-----|--------|---------|------|
| theta | 0.6 | 0.5-0.7 | 隐式度（↑稳定↓精度） |
| omega | 0.95 | 0.85-0.98 | 松弛因子（↓稳定↑收敛） |
| dx | 变化 | 0.1-10m | 网格间距（↓精度↑成本） |
| dt | 自适应 | CFL<0.5 | 时间步长 |
| convergence_tol | 0.1 | 0.01-1.0 | 收敛判据（m³/s） |

### 关键文件

```
solvers/
  ├─ hydrostatic_canal_solver.py    [主求解器，4000+行]
  ├─ canal_solver.py                 [旧求解器，需修复]
  ├─ gate.py                         [闸门/泵站定义]
  └─ hydrostatic_reconstruction.py   [新建，静水重构]

examples/
  └─ example_gate_pump_cascade/      [测试案例]
```

### 快速命令

```bash
# 运行单测试
python -m pytest tests/test_basic_flow.py::test_uniform_flow -v

# 运行所有测试
python -m pytest tests/ -v

# 性能测试
python examples/example_gate_pump_cascade/gate_pump_cascade_advanced.py

# 生成报告
python utils/generate_validation_report.py
```

---

## 🎓 学习资源

### 必读论文（按顺序）

1. **Week 1**: Toro (2001) "Shock-Capturing Methods" - Chapter 5
2. **Week 2**: Audusse et al. (2004) "Hydrostatic Reconstruction" - SIAM
3. **Week 3**: Lai & Khan (2018) "Hybrid FV/FD method"

### 推荐教材

1. LeVeque (2002) "Finite Volume Methods for Hyperbolic Problems"
2. Toro (2009) "Riemann Solvers and Numerical Methods"

### 在线资源

- HEC-RAS Reference Manual (PDF, 免费)
- SWMM 5 Documentation (PDF, 免费)
- Clawpack Tutorials (Python代码示例)

---

## 📞 技术支持

**遇到问题**:
1. 查看本指南"常见问题"部分
2. 检查测试用例是否通过
3. 对比参考论文的算法伪代码
4. 使用性能监控仪表板定位问题

**紧急情况**:
- 回退到上一个工作版本（Git）
- 保守参数（theta=0.7, omega=0.9）
- 增大收敛容差（0.1 → 1.0）

---

## ✅ 每日检查清单

### Day 1-2: 非均匀网格修复
- [ ] 所有导数计算改用局部dx
- [ ] 通过均匀流测试
- [ ] 通过网格加密测试（dx=1m误差<1%）
- [ ] Git commit

### Day 3: 守恒性修复  
- [ ] 删除平滑代码
- [ ] 通过质量守恒测试（<0.01%）
- [ ] 检查数值稳定性
- [ ] Git commit

### Day 8-12: 静水重构
- [ ] 实现reconstruct()方法
- [ ] 实现balanced_source()方法
- [ ] 通过静水测试（<1e-10）
- [ ] 通过稳态测试（<0.1%）
- [ ] Git commit

### Day 15: 综合验证
- [ ] 所有场景测试通过
- [ ] 性能报告生成
- [ ] 代码文档完整
- [ ] **达到0.5%目标**

---

## 🏆 最终交付物

### 代码
- [ ] 修复后的求解器（通过所有测试）
- [ ] 静水重构模块（新增）
- [ ] 完整测试套件（20+测试）
- [ ] 性能基准数据

### 文档  
- [ ] 算法修改说明
- [ ] 静水重构理论文档
- [ ] 用户使用手册
- [ ] 验证报告（含对比图表）

### 验证
- [ ] 与理论解对比（均匀流）
- [ ] 与v4.0对比（精度提升）
- [ ] 与HEC-RAS对比（如有条件）
- [ ] 能量守恒验证

---

**准备好开始了吗？从Day 1开始！** 🚀

**第一步**: `git checkout -b refactor/fix-nonuniform-grid`

**第二步**: 打开 `solvers/canal_solver.py:329`

**Let's fix this!** 💪
