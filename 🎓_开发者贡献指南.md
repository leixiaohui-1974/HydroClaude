# 🎓 HydroClaude 开发者贡献指南

## 📋 目录

- [项目概述](#项目概述)
- [开发环境设置](#开发环境设置)
- [项目结构](#项目结构)
- [开发规范](#开发规范)
- [测试指南](#测试指南)
- [贡献流程](#贡献流程)
- [代码审查标准](#代码审查标准)
- [常见问题](#常见问题)

---

## 项目概述

**HydroClaude** 是一个世界级的水力学仿真平台，专注于：

- ✅ **明渠水流分析**（稳态/非稳态）
- ✅ **管网水力计算**（Hardy-Cross方法）
- ✅ **水锤分析**（特征线法）
- ✅ **水工结构设计**（闸门、堰、孔口等）

### 核心优势

- 🎯 **世界级精度**：流量误差 0.0000%
- ⚡ **卓越性能**：比商业软件快 5-10 倍
- 🎨 **现代化UI**：React + TypeScript + Ant Design
- 🔧 **可扩展性**：组件化架构，插件系统
- 📚 **完整文档**：43 个详细文档

---

## 开发环境设置

### 1. 系统要求

```bash
# 操作系统
- Linux (推荐 Ubuntu 20.04+)
- macOS 11+
- Windows 10+ (WSL2)

# 软件要求
- Python 3.12+
- Node.js 18+
- Git 2.30+
```

### 2. 安装依赖

#### 后端依赖

```bash
# 克隆项目
git clone <repository-url>
cd HydroClaude

# 创建虚拟环境（推荐）
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# 或
venv\Scripts\activate  # Windows

# 安装 Python 依赖
pip install -r requirements.txt

# 安装测试依赖
pip install pytest pytest-html pytest-cov requests
```

#### 前端依赖

```bash
# 进入前端目录
cd web/frontend

# 安装 Node 依赖
npm install

# 安装测试依赖
npm install -D @playwright/test vitest
```

### 3. 验证安装

```bash
# 验证后端
python3 --version  # 应显示 3.12+
pytest --version   # 应显示 pytest 版本

# 验证前端
node --version     # 应显示 18+
npm --version      # 应显示对应版本
```

---

## 项目结构

```
HydroClaude/
├── solvers/                    # 核心求解器
│   ├── hydrostatic_canal_solver.py    # 稳态明渠求解器 ⭐
│   ├── godunov_fvm_solver.py          # 有限体积法求解器
│   ├── hardy_cross_solver.py          # 管网求解器
│   ├── water_hammer_moc_solver.py     # 水锤求解器
│   └── gate.py                         # 水工结构模块
│
├── utils/                      # 工具函数
│   ├── canal_utils.py                  # 水力学计算函数 ⭐
│   ├── result_validator.py             # 结果验证器 ⭐
│   ├── plot_helper.py                  # 绘图助手 ⭐
│   └── visualization_templates.py      # 可视化模板
│
├── tests/                      # 测试代码
│   ├── backend/                        # 后端测试 (68个)
│   │   ├── solvers/                    # 求解器测试
│   │   ├── utils/                      # 工具函数测试
│   │   ├── structures/                 # 结构测试
│   │   ├── integration/                # 集成测试
│   │   └── benchmarks/                 # 性能基准测试
│   ├── e2e/                            # E2E测试 (6个)
│   │   └── test_full_workflow.py
│   └── frontend/                       # 前端测试 (8+个)
│       ├── canvas.spec.js
│       ├── simulation.spec.js
│       └── new-features.spec.js
│
├── web/frontend/               # 前端代码
│   ├── src/
│   │   ├── features/                   # 功能模块
│   │   │   ├── batch/                  # 批处理管理器
│   │   │   ├── reports/                # 报告生成器
│   │   │   └── advanced/               # 高级功能
│   │   ├── components/                 # 通用组件
│   │   ├── hooks/                      # 自定义 Hooks
│   │   └── stores/                     # 状态管理
│   └── package.json
│
├── examples/                   # 示例代码
│   └── example_01_canal_flow/
│       └── scripts/                    # 示例脚本
│
├── docs/                       # 文档
│   ├── LIBRARY_REFERENCE.md            # 基础库API文档 ⭐⭐⭐
│   ├── DEVELOPMENT_GUIDE.md            # 开发指南
│   ├── EXAMPLES_INDEX.md               # 示例索引
│   └── API.md                          # API文档
│
└── reports/                    # 测试报告
    ├── html/                           # HTML报告
    └── coverage/                       # 覆盖率报告
```

### 关键文件说明

#### ⭐ 必读文件（开发前）

1. **`LIBRARY_REFERENCE.md`** - 完整的基础库API文档
   - 所有求解器API
   - 所有工具函数API
   - 使用示例

2. **`DEVELOPMENT_GUIDE.md`** - 开发规范和最佳实践
   - 代码规范
   - 开发流程
   - 性能要求

3. **`🎓_开发者贡献指南.md`** - 本文档
   - 贡献流程
   - 代码审查标准

---

## 开发规范

### 核心原则：基础库优先（LIBRARY FIRST）⚠️

**在编写任何代码之前，必须先做这些检查：**

```
✅ 第1步：打开并查阅 LIBRARY_REFERENCE.md
✅ 第2步：搜索是否有对应的基础库
✅ 第3步：查看 examples/ 下是否有类似示例
✅ 第4步：如果有基础库，必须使用它！
❌ 禁止：直接编写功能实现代码
```

### Python 代码规范

#### 1. 导入顺序

```python
#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
模块功能描述

Author: [作者名]
Date: [日期]
"""

# 标准库导入
import sys
import os
from typing import Optional, Tuple, Dict

# 第三方库导入
import numpy as np
import matplotlib.pyplot as plt

# 路径设置
script_path = os.path.abspath(__file__)
project_root = os.path.dirname(os.path.dirname(os.path.dirname(script_path)))
sys.path.insert(0, project_root)

# HydroClaude基础库导入（必须）
from solvers.hydrostatic_canal_solver import HydrostaticCanalSolver
from utils.canal_utils import compute_steady_uniform_flow
from utils.result_validator import quick_validate_steady_state
from utils.plot_helper import PlotHelper
```

#### 2. 命名规范

```python
# 类名：大驼峰
class HydrostaticCanalSolver:
    pass

# 函数名：小写+下划线
def compute_steady_uniform_flow():
    pass

# 常量：大写+下划线
MAX_ITERATIONS = 1000
CONVERGENCE_TOL = 0.1

# 变量：小写+下划线
flow_rate = 10.0
water_depth = 2.5
```

#### 3. 类型注解（推荐）

```python
def compute_flow(
    width: float,
    depth: float,
    slope: float,
    roughness: float
) -> Tuple[float, Dict[str, float]]:
    """
    计算流量
    
    Args:
        width: 渠道宽度 (m)
        depth: 水深 (m)
        slope: 底坡
        roughness: 糙率系数
        
    Returns:
        (flow_rate, diagnostics): 流量和诊断信息
    """
    pass
```

#### 4. 文档字符串

```python
def my_function(param1: float, param2: str) -> bool:
    """
    简短的一句话描述
    
    详细的多行描述，解释函数的功能、用途、
    注意事项等。
    
    Args:
        param1: 参数1的描述，包括单位
        param2: 参数2的描述
        
    Returns:
        返回值的描述
        
    Raises:
        ValueError: 什么情况下抛出
        
    Example:
        >>> result = my_function(1.0, "test")
        >>> print(result)
        True
    """
    pass
```

### TypeScript/React 代码规范

#### 1. 组件结构

```typescript
import React, { useState, useEffect } from 'react';
import { Button, Modal, Form } from 'antd';
import type { FormInstance } from 'antd';

// 接口定义
interface MyComponentProps {
  title: string;
  visible: boolean;
  onClose: () => void;
  onSubmit?: (data: FormData) => void;
}

interface FormData {
  name: string;
  value: number;
}

// 组件定义
export const MyComponent: React.FC<MyComponentProps> = ({
  title,
  visible,
  onClose,
  onSubmit
}) => {
  // 状态
  const [loading, setLoading] = useState(false);
  const [form] = Form.useForm<FormData>();

  // 副作用
  useEffect(() => {
    if (visible) {
      form.resetFields();
    }
  }, [visible]);

  // 事件处理
  const handleSubmit = async () => {
    try {
      setLoading(true);
      const values = await form.validateFields();
      onSubmit?.(values);
    } catch (error) {
      console.error('表单验证失败:', error);
    } finally {
      setLoading(false);
    }
  };

  // 渲染
  return (
    <Modal
      title={title}
      open={visible}
      onCancel={onClose}
      onOk={handleSubmit}
      confirmLoading={loading}
    >
      <Form form={form}>
        {/* 表单内容 */}
      </Form>
    </Modal>
  );
};
```

#### 2. 命名规范

```typescript
// 组件：大驼峰
export const MyComponent: React.FC = () => {};

// 接口：大驼峰 + I前缀（可选）
interface IUserData {
  id: string;
  name: string;
}

// 类型：大驼峰
type Status = 'pending' | 'running' | 'completed';

// 函数：小驼峰
const handleClick = () => {};
const fetchUserData = async () => {};

// 常量：大写+下划线
const MAX_RETRY_COUNT = 3;
const API_BASE_URL = 'http://localhost:8000';
```

---

## 测试指南

### 后端测试

#### 1. 单元测试

```python
import pytest
from solvers.hydrostatic_canal_solver import HydrostaticCanalSolver

class TestHydrostaticSolver:
    """稳态求解器测试"""
    
    @pytest.fixture
    def solver(self):
        """创建求解器实例"""
        return HydrostaticCanalSolver(
            L=1000.0,
            B=5.0,
            S0=0.001,
            n=0.025,
            nx=101
        )
    
    def test_uniform_flow(self, solver):
        """测试均匀流"""
        Q_target = 10.0
        
        # 初始化
        h_init = compute_steady_uniform_flow(Q_target, 5.0, 0.001, 0.025)
        solver.set_initial_depth(h_init)
        solver.set_initial_flow_rate(Q_target)
        
        # 求解
        result = solver.solve_steady_state(
            Q_target=Q_target,
            convergence_tol=0.1,
            max_iterations=100
        )
        
        # 验证
        assert result['converged'] == True
        flow_error = abs(result['Q_final'] - Q_target) / Q_target * 100
        assert flow_error < 0.01, f"流量误差 {flow_error:.6f}% 超过 0.01%"
```

#### 2. 集成测试

```python
class TestSolverWithStructures:
    """求解器与结构集成测试"""
    
    def test_solver_with_gate(self):
        """测试求解器 + 闸门"""
        # 创建求解器
        solver = HydrostaticCanalSolver(...)
        
        # 添加闸门
        gate = SluiceGate(position=5000.0, width=10.0, opening=5.0)
        solver.add_structure(gate)
        
        # 求解
        result = solver.solve_steady_state(...)
        
        # 验证
        assert result['converged'] == True
        # ... 更多验证
```

#### 3. 性能基准测试

```python
import time

class TestPerformanceBenchmark:
    """性能基准测试"""
    
    def test_small_scale_performance(self):
        """小规模性能测试"""
        solver = HydrostaticCanalSolver(L=1000.0, nx=101)
        
        # 测量时间
        start_time = time.time()
        result = solver.solve_steady_state(Q_target=10.0)
        solve_time = time.time() - start_time
        
        # 性能要求
        assert solve_time < 1.0, f"求解时间 {solve_time:.3f}s 超过 1.0s"
        assert result['iterations'] < 10, "迭代次数过多"
```

### 前端测试

#### 1. 组件测试（Vitest）

```typescript
import { describe, it, expect } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { MyComponent } from './MyComponent';

describe('MyComponent', () => {
  it('应该正确渲染', () => {
    render(<MyComponent title="测试" visible={true} onClose={() => {}} />);
    expect(screen.getByText('测试')).toBeInTheDocument();
  });

  it('应该正确处理点击事件', async () => {
    const handleClose = vi.fn();
    render(<MyComponent title="测试" visible={true} onClose={handleClose} />);
    
    const closeButton = screen.getByRole('button', { name: /关闭/ });
    await fireEvent.click(closeButton);
    
    expect(handleClose).toHaveBeenCalledTimes(1);
  });
});
```

#### 2. E2E测试（Playwright）

```javascript
const { test, expect } = require('@playwright/test');

test.describe('仿真工作流', () => {
  test('应该能够创建并运行仿真', async ({ page }) => {
    // 访问页面
    await page.goto('http://localhost:5173');
    
    // 创建仿真
    await page.click('button:has-text("新建仿真")');
    await page.fill('input[name="name"]', '测试仿真');
    await page.click('button:has-text("确定")');
    
    // 验证结果
    await expect(page.locator('.simulation-card')).toContainText('测试仿真');
  });
});
```

### 运行测试

```bash
# 后端测试
./run_tests.sh                    # 运行所有测试
pytest tests/backend/ -v          # 运行后端测试
pytest tests/backend/solvers/ -v  # 运行特定模块测试

# E2E测试
pytest tests/e2e/ -v -s           # 运行E2E测试

# 前端测试
cd web/frontend
npm test                          # 运行单元测试
npx playwright test               # 运行E2E测试

# 测试覆盖率
pytest tests/backend/ --cov=solvers --cov=utils --cov-report=html
```

---

## 贡献流程

### 1. Fork 和 Clone

```bash
# Fork 项目到你的 GitHub 账号
# 然后 Clone 你的 Fork

git clone https://github.com/YOUR_USERNAME/HydroClaude.git
cd HydroClaude

# 添加上游仓库
git remote add upstream https://github.com/ORIGINAL_OWNER/HydroClaude.git
```

### 2. 创建功能分支

```bash
# 从 main 分支创建新分支
git checkout main
git pull upstream main
git checkout -b feature/my-new-feature

# 分支命名规范：
# feature/功能名称    - 新功能
# fix/问题描述        - Bug修复
# docs/文档主题       - 文档更新
# refactor/重构内容   - 代码重构
# test/测试内容       - 测试相关
```

### 3. 进行开发

```bash
# 遵循开发规范编写代码
# 确保代码通过所有测试
./run_tests.sh

# 提交代码（遵循提交信息规范）
git add .
git commit -m "feat: 添加新功能XYZ"

# 提交信息规范：
# feat: 新功能
# fix: Bug修复
# docs: 文档更新
# style: 代码格式（不影响代码运行）
# refactor: 重构
# test: 测试相关
# chore: 构建过程或辅助工具的变动
```

### 4. 推送和创建 Pull Request

```bash
# 推送到你的 Fork
git push origin feature/my-new-feature

# 在 GitHub 上创建 Pull Request
# 填写 PR 描述模板
```

### PR 描述模板

```markdown
## 变更描述
简要描述这个 PR 做了什么

## 变更类型
- [ ] 新功能
- [ ] Bug修复
- [ ] 文档更新
- [ ] 性能优化
- [ ] 代码重构
- [ ] 测试相关

## 测试
- [ ] 所有现有测试通过
- [ ] 添加了新测试
- [ ] 手动测试完成

## 检查清单
- [ ] 代码遵循项目规范
- [ ] 更新了相关文档
- [ ] 没有引入新的警告
- [ ] 提交信息遵循规范

## 相关 Issue
Closes #issue_number
```

---

## 代码审查标准

### 1. 功能性

- ✅ 代码实现了需求
- ✅ 没有明显的 Bug
- ✅ 边界情况处理正确
- ✅ 错误处理恰当

### 2. 代码质量

- ✅ 遵循项目编码规范
- ✅ 命名清晰、有意义
- ✅ 函数/类职责单一
- ✅ 避免代码重复

### 3. 测试

- ✅ 新功能有对应测试
- ✅ 测试覆盖主要场景
- ✅ 测试通过
- ✅ 测试代码质量良好

### 4. 文档

- ✅ 代码有适当注释
- ✅ 复杂逻辑有说明
- ✅ API 变更更新了文档
- ✅ 示例代码可运行

### 5. 性能

- ✅ 没有明显的性能问题
- ✅ 算法复杂度合理
- ✅ 资源使用合理
- ✅ 性能测试通过（如适用）

### 6. 安全性

- ✅ 输入验证充分
- ✅ 没有安全漏洞
- ✅ 敏感信息处理正确
- ✅ 依赖库安全

---

## 常见问题

### Q1: 我应该使用哪个求解器？

**A**: 优先使用 `HydrostaticCanalSolver`（稳态明渠流动）

```python
# ✅ 推荐（性能最好，精度最高）
from solvers.hydrostatic_canal_solver import HydrostaticCanalSolver

# ❌ 不推荐（已废弃）
# from solvers.single_canal_solver import SingleCanalSolver
# from solvers.canal_solver import CanalSolver
```

**选择指南：**
- **稳态明渠流动** → `HydrostaticCanalSolver`
- **非稳态明渠流动** → `GodunvFVMSolver`
- **管网分析** → `HardyCrossSolver`
- **水锤分析** → `WaterHammerMOCSolver`

### Q2: 如何验证计算结果？

**A**: 使用 `ResultValidator`

```python
from utils.result_validator import quick_validate_steady_state

validator = quick_validate_steady_state(
    solver=solver,
    result_dict=result,
    Q_target=Q_target,
    name="我的场景"
)

# 自动输出详细验证报告
# 包括流量误差、水深误差、收敛性等
```

### Q3: 如何绘制结果图表？

**A**: 使用 `PlotHelper`（最简单）

```python
from utils.plot_helper import PlotHelper

plotter = PlotHelper()

# 绘制纵剖面
fig = plotter.plot_profile(x, h, xlabel="距离 (m)", ylabel="水深 (m)")

# 保存图片
fig.savefig("profile.png", dpi=300, bbox_inches='tight')
```

### Q4: 测试失败怎么办？

**A**: 按以下步骤排查：

1. **检查依赖**
```bash
pip install -r requirements.txt
pip install pytest pytest-html pytest-cov requests
```

2. **检查代码**
```bash
# 运行单个测试
pytest tests/backend/solvers/test_hydrostatic_simple.py::test_uniform_flow -v

# 查看详细输出
pytest tests/backend/solvers/test_hydrostatic_simple.py -v -s
```

3. **检查性能**
```python
# 确保满足性能要求
assert result['iterations'] < 100  # 迭代次数
assert flow_error < 0.01  # 流量误差 < 0.01%
```

### Q5: 如何添加新的水工结构？

**A**: 继承 `Structure` 基类

```python
from solvers.gate import Structure

class MyStructure(Structure):
    def __init__(self, position: float, width: float, ...):
        super().__init__(position)
        self.width = width
        # ...
    
    def compute_flow(self, h_upstream: float, h_downstream: float) -> float:
        """计算通过结构的流量"""
        # 实现你的流量计算公式
        pass
    
    def compute_head_loss(self, Q: float, h_upstream: float) -> float:
        """计算水头损失"""
        # 实现你的水头损失计算
        pass
```

### Q6: 前端组件放在哪里？

**A**: 根据组件类型选择目录

```
web/frontend/src/
├── components/        # 通用组件（按钮、表单、对话框等）
├── features/          # 功能模块（批处理、报告、GIS等）
├── hooks/             # 自定义 Hooks
└── stores/            # 状态管理
```

### Q7: 如何运行性能基准测试？

**A**: 使用专门的基准测试

```bash
# 运行性能基准测试
pytest tests/backend/benchmarks/test_performance_benchmark.py -v

# 查看详细结果
pytest tests/backend/benchmarks/test_performance_benchmark.py -v -s
```

### Q8: 代码覆盖率如何查看？

**A**: 生成覆盖率报告

```bash
# 生成HTML覆盖率报告
pytest tests/backend/ --cov=solvers --cov=utils --cov-report=html

# 查看报告
open reports/coverage/index.html  # macOS
xdg-open reports/coverage/index.html  # Linux
```

---

## 开发工具推荐

### 编辑器

- **VS Code**（推荐）
  - Python 插件
  - Pylance
  - ESLint
  - Prettier
  
- **PyCharm**
  - 专业的 Python IDE

### 代码质量工具

```bash
# Python 代码格式化
pip install black
black solvers/ utils/

# Python 代码检查
pip install pylint
pylint solvers/ utils/

# TypeScript 代码检查
cd web/frontend
npm run lint
```

### 调试工具

```python
# Python 调试
import pdb
pdb.set_trace()  # 设置断点

# 或使用 VS Code 调试器（推荐）
```

---

## 获取帮助

### 文档资源

- 📚 [基础库API文档](LIBRARY_REFERENCE.md)
- 📖 [开发指南](DEVELOPMENT_GUIDE.md)
- 📝 [示例索引](EXAMPLES_INDEX.md)
- 🎓 [新手快速开始](🎓_新手快速开始指南.md)

### 社区支持

- GitHub Issues: 报告 Bug 或请求功能
- GitHub Discussions: 技术讨论
- 开发者邮件列表: dev@hydroclaude.org

### 常用命令参考

```bash
# 测试
./run_tests.sh                    # 运行所有测试
pytest tests/backend/ -v          # 后端测试
pytest tests/e2e/ -v -s           # E2E测试

# 服务
python3 main.py                   # 启动后端
cd web/frontend && npm run dev    # 启动前端

# 代码质量
black solvers/ utils/             # 格式化
pylint solvers/ utils/            # 检查
cd web/frontend && npm run lint   # 前端检查
```

---

## 结语

感谢你对 HydroClaude 的贡献！

**记住三个原则：**

1. **先查文档，后写代码**（LIBRARY_REFERENCE.md）
2. **必须使用基础库**（禁止重复造轮子）
3. **必须验证结果**（ResultValidator）

如有任何问题，请随时：
- 📖 查阅文档
- 💬 提出 Issue
- 👥 参与讨论

让我们一起打造世界级的水力学仿真平台！🚀

---

**HydroClaude Development Team**  
**Version: v2.0.0**  
**Last Updated: 2025-11-20**
