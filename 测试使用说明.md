# 🧪 HydroClaude Web 测试使用说明

## 📚 测试文档索引

### 主要报告
- **🎯_全面测试最终报告.md** - 完整详细的测试报告（640+行）
- **测试完成总结.md** - 快速查阅的测试总结

### 测试脚本
- **browser_test_real.py** - 浏览器自动化测试（需要Chrome）
- **test_all_simulations.py** - 7个仿真场景完整测试
- **simple_simulation_test.py** - 直接引擎功能测试
- **remove_all_emojis.py** - Emoji字符清理工具

---

## 🚀 快速开始

### 1. 启动后端服务

```bash
# 进入后端目录
cd web/backend/api_gateway

# 启动FastAPI服务
python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### 2. 启动前端服务

```bash
# 新开一个终端
cd web/frontend

# 启动Vite开发服务器
npm run dev
```

### 3. 运行测试

#### 方式1: 浏览器自动化测试（推荐）

```bash
# 需要安装Chrome和chromedriver
pip install selenium

# 运行测试
python browser_test_real.py
```

**输出**: 在 `web_test_screenshots/` 目录生成带时间戳的截图

#### 方式2: 直接引擎测试

```bash
# 最简单，无需启动服务
python simple_simulation_test.py
```

**优点**: 绕过API层，直接验证核心功能，无编码问题

#### 方式3: API仿真场景测试

```bash
# 需要后端服务运行
python test_all_simulations.py
```

**输出**: `simulation_test_results.json` 包含详细结果

---

## 📊 查看测试结果

### 浏览器测试截图

```bash
# Windows
explorer web_test_screenshots/

# Linux/Mac
open web_test_screenshots/
```

**截图命名格式**: `{序号}_{描述}_{时间戳}.png`

例如:
- `01_01_homepage_loaded_214349.png` - 首页加载成功
- `02_03_modeling_workspace_214350.png` - 建模工作区
- `03_19_tab_0_214351.png` - 标签页0

### 仿真测试结果

```bash
# 查看JSON结果
cat simulation_test_results.json | python -m json.tool

# 或在文本编辑器中打开
code simulation_test_results.json
```

### 测试报告

```bash
# 查看详细报告（推荐）
code 🎯_全面测试最终报告.md

# 查看快速总结
code 测试完成总结.md
```

---

## 🔧 解决常见问题

### 问题1: ChromeDriver版本不匹配

```bash
# 错误信息: "This version of ChromeDriver only supports Chrome version XXX"

# 解决方案:
# 1. 查看Chrome版本: chrome://version/
# 2. 下载匹配的ChromeDriver:
#    https://chromedriver.chromium.org/downloads
# 3. 将chromedriver.exe放到PATH中或项目目录
```

### 问题2: 前端服务未启动

```bash
# 错误信息: Selenium无法连接到http://localhost:5173

# 解决方案:
cd web/frontend
npm install  # 如果首次运行
npm run dev  # 启动前端服务
```

### 问题3: 后端模块导入错误

```bash
# 错误信息: No module named 'solvers'

# 解决方案:
# 确保在项目根目录运行测试脚本
cd E:\OneDrive\Documents\GitHub\Test\HydroClaude
python simple_simulation_test.py
```

### 问题4: Windows编码问题

```bash
# 错误信息: 'gbk' codec can't encode character

# 解决方案1: 使用环境变量
$env:PYTHONIOENCODING="utf-8"
python simple_simulation_test.py

# 解决方案2: 使用direct测试（推荐）
# simple_simulation_test.py 已内置编码处理
python simple_simulation_test.py
```

---

## 📦 测试依赖安装

### Python依赖

```bash
pip install -r requirements.txt

# 或手动安装关键包
pip install fastapi uvicorn selenium requests numpy
```

### Chrome和ChromeDriver

```bash
# Windows:
# 1. 下载Chrome: https://www.google.com/chrome/
# 2. 下载ChromeDriver: https://chromedriver.chromium.org/

# Linux (Ubuntu/Debian):
sudo apt-get update
sudo apt-get install -y chromium-browser chromium-chromedriver

# macOS:
brew install --cask google-chrome
brew install chromedriver
```

---

## 🎯 测试覆盖范围

### 1. 后端API测试
- ✅ 健康检查端点
- ✅ 欢迎页面
- ✅ 引擎信息查询
- ✅ 仿真创建
- ✅ 仿真状态查询
- ✅ 仿真结果获取
- ✅ 仿真列表
- ✅ 仿真删除

### 2. 前端UI测试
- ✅ 页面加载
- ✅ 标题和Logo
- ✅ 标签页切换
- ✅ 建模工作区
- ✅ 仿真工作区
- ✅ 响应式布局
- ✅ 控制台错误检查
- ✅ 按钮和交互元素

### 3. 核心引擎测试
- ✅ Godunov-FVM求解器
- ✅ Numba JIT加速
- ✅ 数值精度验证
- ✅ 收敛性检查
- ✅ 质量守恒验证

### 4. 错误处理测试
- ✅ 无效输入处理
- ✅ 网络错误恢复
- ✅ 超时处理
- ✅ 异常捕获

---

## 📈 测试结果解读

### 引擎测试指标

```python
# 正常指标范围
Duration: 3-10秒 (取决于网格大小)
Max Depth: 合理范围内 (通常2-10m)
Max Velocity: < 10 m/s (明渠流)
Converged: True (必须收敛)
Mass Error: < 1.0 (质量守恒)
```

### 浏览器测试检查项

- **✅ 页面加载**: 3秒内完成
- **✅ 无JS错误**: console.log为空或仅警告
- **✅ 元素可见**: 关键按钮和表单存在
- **✅ 交互响应**: 点击、输入正常

---

## 🛠️ 自定义测试

### 添加新的测试场景

编辑 `test_all_simulations.py`:

```python
# 在TEST_SCENARIOS列表中添加
{
    "name": "场景8: 你的测试名称",
    "description": "测试描述",
    "config": {
        "width": 15.0,  # 渠道宽度
        "length": 1500.0,  # 渠道长度
        "n_cells": 150,  # 网格数
        "manning_n": 0.03,  # 糙率
        "slope": 0.002,  # 坡度
        "t_end": 100.0,  # 模拟时间
        # ... 其他参数
    }
}
```

### 修改浏览器测试

编辑 `browser_test_real.py`:

```python
def your_custom_test(driver, screenshot_dir, counter):
    """自定义测试函数"""
    print(f"\n[{counter[0]}] Your test description")
    
    # 执行操作
    element = driver.find_element(By.ID, "your-element-id")
    element.click()
    
    # 截图
    save_screenshot(driver, screenshot_dir, f"{counter[0]}_your_test")
    counter[0] += 1
```

---

## 📞 支持和反馈

### 测试问题报告

如果测试失败，请提供:
1. 错误消息完整文本
2. 测试环境（OS、Python版本、浏览器版本）
3. 相关截图
4. `simulation_test_results.json`（如果有）

### 查看日志

```bash
# 后端日志
cd web/backend/api_gateway
uvicorn main:app --log-level debug

# 浏览器测试详细输出
python browser_test_real.py 2>&1 | tee test_output.log
```

---

## ✅ 测试检查清单

运行测试前确认:

- [ ] Python 3.8+ 已安装
- [ ] 项目依赖已安装 (`pip install -r requirements.txt`)
- [ ] Chrome浏览器已安装
- [ ] ChromeDriver版本匹配
- [ ] 后端服务已启动 (port 8000)
- [ ] 前端服务已启动 (port 5173)
- [ ] 在项目根目录运行测试

---

**最后更新**: 2025-11-12  
**版本**: 1.0  
**测试框架**: Selenium + Requests + Direct Engine Call  
**适用平台**: Windows, Linux, macOS

---

**Generated by HydroClaude Testing Team**






