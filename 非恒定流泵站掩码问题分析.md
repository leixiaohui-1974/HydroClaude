# 非恒定流泵站掩码问题分析

**发现时间**: 2025-10-26  
**问题**: 泵站掩码在非恒定流中导致流量翻倍

---

## 问题根源

### 当前实现

在`step_preissmann()`中（第671-678行）：

```python
# 获取泵站区域掩码
pump_mask = self._get_pump_region_mask()

# 更新（排除泵站区域）
for i in range(self.nx):
    if pump_mask[i]:
        # 泵站区域：保持不变
        continue
    
    # 连续性方程和动量方程更新...
```

### 问题

**泵站掩码包括3个节点**（idx-1, idx, idx+1），这些节点：
1. ❌ 在Preissmann更新中被跳过
2. ❌ 保持旧的hu值（从稳态）
3. ❌ 与周围节点不匹配

**后果**：
- 稳态时：泵站节点hu被全局强制为Q_target/B，一切正常
- 非恒定流时：泵站节点hu不更新，导致累积误差

---

## 实验证据

```
稳态初始化: Q_avg = 10.0000 m³/s  ✓
Preissmann步后: Q_avg = 20.2188 m³/s  ✗ (翻倍!)
```

泵站附近流量分布异常：
```
idx24: 10.00 m³/s  (上游，正常)
idx25: 10.00 m³/s  (泵站，被掩码)
idx26: 10.00 m³/s  (泵站+1，被掩码)
idx27: 41.58 m³/s  (下游，异常!)
```

---

## 解决方案

### 方案A: 非恒定流中不使用泵站掩码（推荐）

**原理**：
- 稳态求解需要掩码（配合全局流量强制）
- 非恒定流不需要掩码（泵站通过_apply_pump_head_jump处理）

**修改**：

```python
def step_preissmann(self, dt, enforce_bc=False, Q_in=None, h_out=None,
                    use_pump_mask=True):  # 新增参数
    # ...
    
    # 获取泵站区域掩码
    if use_pump_mask:
        pump_mask = self._get_pump_region_mask()
    else:
        pump_mask = np.zeros(self.nx, dtype=bool)  # 全False
    
    # ... 后续逻辑不变
```

**调用时**：
```python
# 稳态求解：使用掩码
h_new, hu_new = solver.step_preissmann(dt, use_pump_mask=True)

# 非恒定流：不使用掩码
h_new, hu_new = solver.step_preissmann(dt, enforce_bc=True, 
                                       Q_in=10.0, h_out=2.0,
                                       use_pump_mask=False)  # ⭐关键
```

### 方案B: 在泵站节点也更新，然后覆盖

保持当前逻辑，但在更新后立即应用泵站条件。

**缺点**：增加计算量，逻辑复杂

---

## 实施步骤

1. 修改`step_preissmann()`添加`use_pump_mask`参数
2. 修改稳态求解器，明确使用`use_pump_mask=True`
3. 修改非恒定流，使用`use_pump_mask=False`
4. 验证修复效果

---

## 预期效果

- 无结构物系统：保持当前良好表现 ✓
- 单闸门系统：保持当前良好表现 ✓
- 单泵站系统：修复流量守恒问题 ✓✓✓
- 多结构物系统：修复流量守恒问题 ✓✓✓

---

*报告生成时间: 2025-10-26*  
*下一步: 实施方案A*
