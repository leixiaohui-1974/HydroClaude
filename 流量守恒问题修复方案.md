# 流量守恒问题修复方案

**日期**: 2025-10-26  
**问题**: 非恒定流模拟中流量不守恒  
**状态**: 🔴 **严重问题**

---

## 🔍 问题诊断结果

### 测试1: 无结构物稳态流 ✅
```
流量: 10.0000±0.0000 m³/s
结论: 稳态求解器工作正常
```

### 测试2: 单闸门稳态流 ✅
```
流量: 10.0000±0.0000 m³/s
闸门前后流量差: 0.0000 m³/s
结论: 闸门内部边界条件正确
```

### 测试3: 无结构物非恒定流 ❌
```
初始: Q_avg = 10.0000 m³/s
10步后: Q_avg = 15.0026 m³/s (+50%)

入流: 10.0000 m³/s (固定)
出流: 10.36 → 14.88 m³/s (持续增长)

结论: 非恒定流Preissmann步存在问题
```

---

## 🎯 问题定位

**根本原因**: `step_preissmann()` 方法在非恒定流模拟中未能保证流量守恒

**证据**:
- 稳态求解器 ✓ 正常（流量守恒）
- 结构物处理 ✓ 正常（闸门/泵站不产生流量）
- Preissmann步 ✗ **有问题**（即使无结构物也不守恒）

**具体问题**:
1. 边界条件在迭代过程中未正确应用
2. 松弛因子可能导致流量累积
3. 泵站区域的掩码和强制可能与边界条件冲突

---

## 🔧 修复方案

### 方案A: 修复Preissmann步边界条件（推荐）

**问题**: `step_preissmann(enforce_bc=True)` 参数未生效

**分析代码**:
```python
def step_preissmann(self, dt, enforce_bc=False, Q_in=None, h_out=None):
    # ... 迭代 ...
    for iter in range(max_iter):
        # 计算通量
        # 更新h_new, hu_new
        
        # 边界条件
        if enforce_bc:
            # 入流边界
            if Q_in is not None:
                hu_new[0] = Q_in / self.B
            # 出流边界  
            if h_out is not None:
                h_new[-1] = h_out
```

**问题**: 
- 边界条件可能在后续被松弛覆盖
- 泵站掩码可能影响边界节点
- 需要在松弛**之后**再次强制边界

**修复**:
```python
def step_preissmann(self, dt, enforce_bc=False, Q_in=None, h_out=None):
    for iter in range(max_iter):
        # ... 更新 ...
        
        # 松弛
        h_new = relax * h_new + (1-relax) * h_old_iter
        hu_new = relax * hu_new + (1-relax) * hu_old_iter
        
        # ⭐ 关键：在松弛后再次强制边界条件
        if enforce_bc:
            if Q_in is not None:
                hu_new[0] = Q_in / self.B
                h_new[0] = self.h[0]  # 保持上游水深
            if h_out is not None:
                h_new[-1] = h_out
                # 不强制hu_new[-1]，让其自然响应
```

### 方案B: 使用守恒型格式（备选）

使用严格守恒的数值格式（如有限体积法）替代当前的Preissmann格式。

**优点**: 天然保证流量守恒  
**缺点**: 需要大量重构

### 方案C: 增加流量守恒约束（补丁）

在每个时间步后强制调整流量。

**缺点**: 不是根本解决方案，会影响精度

---

## 📋 修复步骤

### 步骤1: 检查并修复边界条件强制

```python
# 文件: solvers/hydrostatic_canal_solver.py
# 方法: step_preissmann()

# 确保边界条件在松弛后强制
if enforce_bc:
    # 上游边界：固定流量
    if Q_in is not None:
        hu_new[0] = Q_in / self.B
    
    # 下游边界：固定水深
    if h_out is not None:
        h_new[-1] = h_out
```

### 步骤2: 检查泵站掩码是否影响边界

```python
# 确保泵站掩码不包括边界节点
pump_mask[0] = False
pump_mask[-1] = False
```

### 步骤3: 验证修复

运行测试：
1. 无结构物非恒定流
2. 单结构物非恒定流
3. 多结构物非恒定流

期望结果：
- 流量守恒误差 < 1%
- 出流 ≈ 入流（±1%）

---

## ⚠️ 注意事项

### 稳态 vs 非恒定流

**稳态求解器**:
- 使用 `solve_steady_state()`
- 全局强制流量：`hu[:] = Q_target / B`
- ✓ 流量守恒良好

**非恒定流模拟**:
- 使用 `step_preissmann(enforce_bc=True)`
- 仅边界强制流量
- ✗ **流量不守恒**（需修复）

### 关键差异

稳态求解器在每次迭代中**全局强制流量**：
```python
if pump_mask.any():
    self.hu[~pump_mask] = Q_target / self.B
```

非恒定流不应这样做（会破坏动态响应），但**必须保证质量守恒**。

---

## 🧪 测试用例

### 测试1: 守恒型算例
```python
# 无结构物，恒定边界
Q_in = 10.0 m³/s
h_out = 2.0 m
运行100步

预期: Q_avg保持10.0±0.1 m³/s
```

### 测试2: 上游流量阶跃
```python
# t<600s: Q_in = 10.0
# t≥600s: Q_in = 15.0
运行200步

预期: 
- t<600s: Q_avg ≈ 10.0
- t≥600s: Q_avg ≈ 15.0
- 误差 < 5%
```

### 测试3: 串联结构物
```python
# 3闸门 + 2泵站
Q_in = 10.0 m³/s (恒定)
运行150步

预期:
- 入流 ≈ 出流 ≈ 10.0 m³/s
- 误差 < 5%
```

---

## 🏆 验收标准

### 最低要求
- ✓ 无结构物流量守恒误差 < 1%
- ✓ 单结构物流量守恒误差 < 2%
- ✓ 多结构物流量守恒误差 < 5%

### 理想目标
- ✓ 所有情况流量守恒误差 < 1%
- ✓ 符合SWMM/HEC-RAS标准

---

## 📊 优先级

| 任务 | 优先级 | 估计时间 |
|-----|-------|---------|
| 修复边界条件强制 | 🔴 最高 | 1h |
| 检查泵站掩码 | 🔴 最高 | 0.5h |
| 验证无结构物 | 🟠 高 | 0.5h |
| 验证单结构物 | 🟠 高 | 0.5h |
| 综合测试 | 🟡 中 | 1h |

**总计**: 约3.5小时

---

*报告生成时间: 2025-10-26*  
*下一步: 修复step_preissmann边界条件强制*
